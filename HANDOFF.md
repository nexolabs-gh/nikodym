# HANDOFF

_Última actualización: 2026-06-26 23:20 · repo privado `nexolabs-gh/nikodym` · **🔴 BLOQUEO R0 — GitHub Actions SIN CUOTA (billing).** El CI no arranca NINGÚN job ("recent account payments have failed or your spending limit needs to be increased"): runs `28274537776` (cierre A3) y `28275055048` (A4(0)) fallan en 2-5 s, todos los jobs. **A4(0) ✅ HECHO EN CÓDIGO** (`d932f52`, cobertura global→**100%**, gates locales verdes, revisor✓) pero su CI quedó rojo POR BILLING, no por código. Mientras el billing esté agotado NO se puede validar ni avanzar la cola (todo push queda rojo) → **latido PARADO DE VERDAD (2026-06-26 23:20).** ⚠ El latido REAL **NO era launchd** (la corrida previa paró un LaunchAgent inexistente); es el **scheduler-loop en tmux `autodev-cron` + watchdog `autodev-watchdog`**, que siguió vivo y lanzó OTRA corrida Opus en vano (esta). Esta corrida lo paró de verdad: maté el watchdog + matador `setsid` del scheduler. Acción de Cami (R0): subir el spending limit / regularizar el pago en GitHub → Settings → Billing & plans, luego **RELANZAR el latido en tmux** (ver "🤖 Modo autónomo"). Último CI verde: `9863060` (A3/B4.4, run `28274334345`). Ver "🆕 Cola autónoma ACTIVA"._

## 🤖 Modo autónomo (rutina)
**Estado:** `IDLE`  _(IDLE = libre · `RUNNING [ts]` = corrida en curso. El lock de OS del wrapper garantiza exclusión; un `RUNNING` visto al arrancar = corrida muerta → rescatar, no abortar.)_
**⚠ BLOQUEADO (R0 — billing GitHub Actions, desde 2026-06-26 22:42):** el CI no arranca ningún job por cuota/pago agotado de GitHub Actions. **NO construir ni pushear código** hasta que Cami arregle el billing (todo push quedaría rojo sin poder validarse). 🔴 **CORRECCIÓN (2026-06-26 23:20): el latido NO es launchd.** El LaunchAgent `cl.nexolabs.autodesarrollo.nikodym` NO estaba cargado (launchd lleva descargado por TCC desde el 24-jun, ver ORQUESTA.md) → la corrida previa paró el mecanismo equivocado y el latido siguió vivo. El latido REAL es el **scheduler-loop en tmux `autodev-cron`** + **watchdog en tmux `autodev-watchdog`**. Esta corrida lo **paró de verdad** para no quemar Opus contra un CI muerto: maté el watchdog (el relanzador) + dejé un matador `setsid` que mata el scheduler y cierra `autodev-cron` al terminar el turno. **REANUDAR (Cami) tras arreglar billing:** (1) GitHub → Settings → Billing & plans → subir spending limit / regularizar pago; (2) **relanzar el latido en tmux** (NO launchd): `cd /Users/camilogonzalez/Documents/Proyectos/AutoDesarrollo && tmux new-session -d -s autodev-cron "MAX_CORRIDA=5400 LIMITE_VACIAS=3 bash scripts/scheduler-loop.sh 2>&1 | tee -a logs/scheduler.log"` + (opcional) `tmux new-session -d -s autodev-watchdog "bash scripts/watchdog-scheduler.sh"`; (3) opcional: pushear un commit vacío para re-disparar el CI y confirmar `main` verde. El código de `main` está SANO (gates locales 100% verdes en `d932f52`).
**Última corrida:** 2026-06-26 23:20 (autónoma headless DanIA) · **R0 billing PERSISTE + LATIDO PARADO DE VERDAD.** Verifiqué con datos: el run más reciente `28276583992` (commit de cierre `63596c9`, 02:59 UTC) **falla en 5 s con el mensaje literal de billing** → el billing NO se ha arreglado; último CI verde sigue siendo `28274334345` (`9863060`). Hallazgo material: **el latido seguía vivo** — la corrida previa creyó pararlo vía `launchctl bootout` de un LaunchAgent que ni estaba cargado, pero el latido real (scheduler-loop en tmux `autodev-cron` + watchdog) siguió y lanzó ESTA corrida Opus en vano. Lo paré de verdad: maté `autodev-watchdog` + matador `setsid` que cierra `autodev-cron` al terminar el turno. **NO construí código** (cualquier push quedaría rojo por billing); esta corrida solo verificó el bloqueo, corrigió el mecanismo/instrucción de reanudación del latido y avisó a Cami. Lock IDLE. **Próximo (SOLO tras desbloquear billing y RELANZAR el latido en tmux): A4(a) B4.1 y resto de A4 → A5.**
Penúltima corrida: 2026-06-26 22:42 (autónoma) · cierre de A4(0) (`63596c9`) + detección del bloqueo R0 billing · A4(0) ✅ HECHO EN CÓDIGO (`d932f52`, CI rojo POR BILLING, no por código; gates locales 100% verdes: 633 tests, 0 miss).
**✅ COLA AUTÓNOMA ACTIVA — VENTANA 16h (2026-06-26).** Cami autorizó avance autónomo amplio: **(1)** aprobó **SDD-27 (eda)** → implementar EDA; **(2)** aprobó **B4.4 Windows + B4.5 Py3.13** para v0.1.0; **(3)** MANDATO textual: _"haz las tandas (SDD) necesarias y luego las aplicas; la revisión la hago yo al final cuando la librería esté lista"_. → trabajar la **"🆕 Cola autónoma ACTIVA (2026-06-26)"** de abajo, en orden: **A1 B5 EDA → A2 B4.5 → A3 B4.4 → A4 deudas F1 → A5 continuación F1 diseña-y-aplica (binning..report)**. ⛔ NO toques **R0-C (release público v0.1.0)**: es lo único irreversible, queda para Cami al volver. El **diseño autónomo va SIEMPRE anclado a la normativa CMF + SDDs aprobados**, marcando cada decisión autónoma para la revisión final. El **revisor del paso 6.5** (adversarial, motor cruzado) es **OBLIGATORIO** en cada push (probado E2E con `baac2a9`, `019ca1b`, `f552c72`). Si una fase no se puede diseñar con seguridad, márcala bloqueada y pasa a la siguiente; si no queda NADA, auto-pausa limpia (sin churn).
Penúltima corrida: 2026-06-26 20:43 (autónoma) · **A2 / B4.5 Python 3.13** · `1c9623b` · ✓ HECHO (constraint uv `ortools>=9.12` + matriz del CI con 3.13; verificado en Python 3.13 real local; 629 tests, 100%; revisor✓; CI verde run `28273170069`) _(detalle en "Histórico — A2 / B4.5" abajo)._
Último ítem de trabajo (código): 2026-06-26 21:53 · **A4(0) — test de cobertura `eda/univariate.py:240->263`** · `d932f52` · ✓ HECHO EN CÓDIGO (CI rojo POR BILLING, no por código) _(test de columna categórica dtype string 100% NaN sobre población elegible no vacía → ejercita la rama `non_missing` vacío de `_categorical_profile`, antes sin cubrir; cobertura global 99.98%→**100%**; solo test, sin tocar fuente; revisor adversarial Claude motor-cruzado APROBÓ. Lo pusheó una corrida anterior que NO alcanzó a cerrar el HANDOFF; esta corrida lo verificó [gates locales 100% verdes] y lo cerró. **Su CI [run `28275055048`] falló por billing**, igual que TODO push posterior al agotamiento de cuota.)._ Penúltimo ítem de código: 2026-06-26 21:27 · **A3 / B4.4 Windows** · `9863060` · ✓ HECHO _(`Study.save` Windows-safe + `windows-latest` en CI; `core/study.py` 100%, 632 tests; revisor✓; **CI verde run `28274334345` — ÚLTIMO run que GitHub permitió correr antes del agotamiento de cuota**)._
Ciclo en la skill **auto-desarrollo** §2 (rescate §4); playbook en `docs/AUTONOMY.md`; bitácora en `AUTONOMY-LOG.md`. Worker Codex en tmux `nikodym`; maestro fresco por cron horario (:17).

## Backlog priorizado (cola autónoma)
> La rutina consume esto en orden, de arriba a abajo. Marca `[x]` al terminar; **NO** borres ni renumeres.
> Cada ítem es autocontenido = un módulo + sus tests, dejando los 4 gates verdes (cobertura 100%). El worker
> deja el árbol verde **sin commitear**; el maestro revisa, commitea y pushea (R7).

### 🆕 Cola autónoma ACTIVA (2026-06-26) — VENTANA 16h · consumir ESTO primero, en orden
> Los ítems 1–12 de abajo están `[x]` (histórico F0+FIXES). La rutina debe trabajar AHORA esta cola A1→A5.
> Diseño SIEMPRE anclado a la normativa CMF/IFRS9 (`docs/normativa_cmf_parametros.md`) + ESPECIFICACIONES + SDDs aprobados;
> marca cada decisión de diseño autónoma en una sección "Decisiones para revisión de Cami" del SDD, para su revisión final.
> Un trozo/commit, 4 gates + regulatorio + núcleo liviano verdes, revisor adversarial OBLIGATORIO, esperar `gh run` CI VERDE antes de cerrar.

- [x] **A1 — B5 EDA ✅ COMPLETA 7/7 (`eda/`, SDD-27 APROBADO por Cami 2026-06-26) → TROCEADO en B5.1–B5.7 (planificador, 2026-06-26 14:45); los 7 trozos ✅ (último B5.7 `eaeb78e`, 2026-06-26 19:52).** Fase de análisis exploratorio orientado a riesgo. **R0: ninguno** (SDD-27 aprobado; sus decisiones abiertas §12 son de frontera/integración, resolubles con el default del propio SDD — ver NOTAS). La rutina consume **B5.1→B5.7 EN ORDEN**; la cabecera A1 se marca `[x]` cuando los 7 estén `[x]`. Núcleo liviano intacto (import perezoso `nikodym.eda`, patrón `data`). SDD-27 **solo diagnostica, NO transforma** datos para el modelo. ⚠ Corrección del troceo sugerido: el **PSI NO vive en `eda`** (es SDD-11 sobre el *score*); `eda` solo hace CV/drift/tendencia sobre la tasa de default cruda.
  - [x] **B5.1** ✅ (`3e4b7e5`, CI verde run `28260621149`, revisor✓) — `eda/config.py` (`EdaConfig` + sub-configs `DefaultRateConfig`/`TemporalStabilityConfig`/`UnivariateConfig`/`QualityConfig`/`SamplingConfig`) + `eda/exceptions.py` (`EdaError(NikodymError)`) + cableado de la sección `eda` en `NikodymConfig`: hook diferido `_EDA_CONFIG_CLS` + `field_validator("eda", mode="before")` en `core/config/schema.py` y registro del hook en `eda/__init__.py` (espejo EXACTO de `data`, `_DATA_CONFIG_CLS`). Todos los campos con `title`+`description`+`ge/le`/`Literal`+metadatos `ui_*`; `NikodymBaseConfig` (`extra="forbid"`, `frozen`); `type: Literal["standard"]="standard"`. **`eda` NO es INFRA** → entra al `config_hash` (a diferencia de audit/governance/tracking). `EdaConfig()` construible con defaults. Núcleo liviano intacto. SDD-27 §5/§8/§10. + `tests/unit/test_eda_config.py` (round-trip YAML, `config_hash`, `nikodym_config_strategy(sections=["eda"])`, rechazo `n_quantile_bins=1`→`ConfigError`, golden defaults).
  - [x] **B5.2** ✅ (`b163979`, CI verde run `28263453699`, revisor✓ tras 2 ciclos) — `eda/default_rate.py` (`DefaultRateAnalyzer` + `DefaultRateResult`): tasa de default por período/cohorte sobre la población elegible (`target∈{0,1}`, `<NA>` excluido del denominador). `compute(frame, *, target_col, audit=None)`; eje `period` (discretiza `date_col` con `to_period(freq)`, `EdaError` si ambiguo/ausente) o `cohort`; tabla `by_period` cols `period|n_total|n_eligible|n_bad|default_rate` (+flag `low_confidence` si `n_eligible<min_obs_per_period`); `overall_rate` **ponderado por elegibles**; `n_eligible==0`→`default_rate=NaN` marcado, sin `ZeroDivisionError`. No-estimador (`from_config`, sin `fit/predict`). SDD-27 §4/§6/§7/§8. + `tests/unit/test_eda_default_rate.py` (canónicos: 100 elegibles/10 malos→`0.10` exacto; `overall_rate` ponderado a mano; invariantes `0≤n_bad≤n_eligible≤n_total`; Hypothesis `0≤rate≤1` e invarianza a reordenar filas).
  - [x] **B5.3** ✅ (`adeca5a`, CI verde run `28265966509`, revisor✓ tras 3 ciclos) — `eda/stability.py` (`TemporalStabilityAnalyzer` + `StabilityResult`): estabilidad temporal **descriptiva** de la tasa de default. `assess(default_rate, *, audit=None)`: `cv=σ/μ`, `max_relative_drift`, `trend_slope` (OLS simple, solo orientación, sobre tasas sin `low_confidence`); `flagged = (indicador[cfg.metric] > cfg.threshold)`. Si `flagged`: `log_decision(regla="estabilidad_temporal", umbral, valor, accion="senalar_redesarrollo")` vía `AuditableMixin` — **SEÑALA, NO aborta**. `<2 períodos`→`cv=NaN`, `flagged=False`, `log_decision(...accion="no_evaluable")`. ⚠ **NO calcula PSI/KS/AUC/Gini** (SDD-11, D-EDA-1). SDD-27 §3/§4/§7/§9. + `tests/unit/test_eda_stability.py` (serie que dispara umbral→`flagged=True` + **exactamente un** `AuditEvent(kind="decision")` con `InMemoryAuditSink`; `<2 períodos`→`no_evaluable`; barajado de filas; período NaT excluido; eje cohorte rechazado; tasa inf excluida; golden cv/drift/slope a mano). **🔴 DECISIÓN AUTÓNOMA para revisión de Cami:** `assess` se **restringió al eje temporal `axis="period"`** (lanza `EdaError` español en cohorte). Motivo: el revisor adversarial reprodujo que ordenar cohortes por su etiqueta de display (string/int/mixto) invierte el signo de `trend_slope` (señal de redesarrollo errada y silenciosa) o lanza `TypeError`; el `trend_slope` (OLS sobre índice secuencial) solo tiene sentido cronológico sobre `period`. Alternativa si Cami quiere estabilidad por cohorte: ordenar por la etiqueta estable de B5.2 (`_stable_label`), no por el display. La señal `flagged` sigue siendo **one-sided** literal (`> threshold`, fiel al SDD §4.2/§7.4): una caída fuerte de la tasa NO la marca; revisar si el negocio quiere two-sided (cambiaría el SDD, no el código).
  - [x] **B5.4** ✅ (`7a10a0a`, CI verde run `28268018692`, revisor✓ tras 1 ciclo) — `eda/univariate.py` (`UnivariateProfiler` + `UnivariateResult`): perfil **descriptivo** feature↔target. `profile(frame, *, target_col, columns, audit=None)` sobre la población elegible (`target∈{0,1}`); numéricas→`pandas.qcut(col, n_quantile_bins, duplicates="drop")` (tramo explícito "missing" al final; numérica constante→1 tramo `[v, v]`); categóricas→un tramo por nivel (clave estable `_stable_label`, orden lexicográfico determinista), freq<`rare_level_threshold`→`"_otros_"`, NaN→"missing"; tabla por columna `tramo|n|coverage|default_rate` (`n`=elegibles del tramo, `coverage`=n/Σn suma 1.0, `default_rate`=n_bad/n NaN si n==0); `descriptive_iv` (dict) **solo si `compute_descriptive_iv`** (Laplace α=0.5, K=tramos, etiquetado `pre-binning`, opt-in default off, D-EDA-3), si no `{}`. Reusa el patrón de `default_rate.py` (`_eligible_mask`/`_bad_mask`/`_stable_label`/`_default_rate_series`/`_validate_*`). ⚠ **DESCRIPTIVO por cuantiles fijos, NO el binning supervisado de SDD-06; NO transforma ni selecciona.** SDD-27 §3/§4/§6/§7/§8. + `tests/unit/test_eda_univariate.py` (IV descriptivo golden 2×2=`1.0426…` a mano; `coverage` suma 1.0; Σn==elegibles; 100% missing→tramo "missing"; invariancia a reordenar filas; determinismo + muestra reproducible vía `SeedManager(42).generator_for("eda")`; ±inf→"missing" preserva Σn/coverage; Hypothesis). **🔴 DECISIÓN AUTÓNOMA (frontera, revisión de Cami): `profile` es función PURA sin muestreo/`rng`; el muestreo + `log_decision(regla="muestreo_eda")` se difieren a B5.6 `EdaStep`** (la firma SDD §4 de `profile` no recibe `rng`; `SamplingConfig` vive en `EdaConfig`, no `UnivariateConfig`). **Revisor adversarial halló 1 bug MATERIAL (gates+100% no lo vieron): `±inf` en numérica → `qcut` perdía filas finitas silenciosamente (Σn≠elegibles, coverage≠1.0)** → fix `replace([np.inf,-np.inf], np.nan)` antes de `missing` (espejo `stability.py:155`). **Nitpicks NO materiales → deuda menor F1:** doble copia (frame completo + cada serie por columna, `univariate.py:108/121`); el `replace` de inf corre siempre aunque no haya infinitos (simetría con `stability.py`, dejar como está).
  - [x] **B5.5** ✅ (`3eb639d`, CI verde run `28269341802`, revisor✓ a la 1ª) — `eda/quality.py` (`DataQualityProfiler` + `QualityResult`): diagnóstico descriptivo `by_column` (1 fila/columna) cols `col|dtype|missing_rate|cardinality|near_constant|near_unique|high_cardinality`. No-estimador `from_config`; `profile(frame, *, audit=None)` (diagnostica TODAS las columnas, sin `target_col`/`columns`) sobre copia defensiva; valida frame no vacío + índice único (reusa `_validate_non_empty_frame`/`_validate_unique_index` de `default_rate`) fail-fast con `EdaError`. `missing_rate`=(#NaN tras `±inf`→NaN)/#filas; `cardinality`=distintos finitos no-NaN; `near_constant`=un valor concentra ≥`near_constant_threshold` de los no-nulos; `high_cardinality`=categórica (object/category/string) >`high_cardinality_threshold` niveles (numéricas→False); `near_unique` ver decisión de frontera. `±inf` neutralizado **solo en numéricas** (`series.replace([np.inf,-np.inf],np.nan)`, lección B5.4). `QualityResult` frozen, dtypes del `by_column` fijados con `astype` (golden estable). Reexport perezoso en `eda/__init__.py` (núcleo liviano intacto). ⚠ **SOLO reporta, NO elimina** (SDD-07). SDD-27 §3/§4/§7. + `tests/unit/test_eda_quality.py` (golden por columna no-tautológico; 100% missing→`missing_rate==1.0`/`cardinality==0`; ±inf→missing dedicado; no muta el frame; reorden de filas; índice duplicado/frame vacío→`EdaError`; Hypothesis). **🔴 DECISIÓN AUTÓNOMA (frontera, revisión de Cami):** `QualityConfig` NO tiene umbral propio para `near_unique` → se usó `near_unique = (n_no_nulos>0) and (cardinality/n_no_nulos ≥ near_constant_threshold)`, documentada en el docstring del módulo. NO se añadió config nueva (habría roto el golden `config_hash` de B5.1). Si Cami quiere afinarlo, el campo propio (p.ej. `near_unique_threshold`) cambiaría el `config_hash`. **Nitpicks del revisor (NO materiales → deuda menor F1):** (1) `near_unique` puede dar `True` sobre una columna CONSTANTE con n minúsculo (`cardinality≤1`) — inocuo en datos reales (con N grande un constante da ratio≈0), solo-reporta; afinar con `cardinality>1 and …` si se desea; (2) faltan tests de `near_unique` sobre constante-con-1-no-nulo y de dtypes `string`/`Int64` nullable (los reales de SDD-02; verificados a mano, funcionan); (3) `profile` no rechaza nombres de columna duplicados (a diferencia de `univariate.py`); solo en uso standalone patológico.
  - [x] **B5.6** ✅ (`ea0463e`, CI verde run `28270902476`, revisor✓ a la 1ª) — `eda/figures.py` (`FigureSpec`: `kind`∈{line,bar,heatmap}, `title` español, `data`, `x`, `y`, `series`) + `eda/step.py` (`@register("standard", domain="eda")` `EdaStep(AuditableMixin)` + `EdaResult`) + cableado `Study.run(steps=[...,"eda"])`. `EdaStep`: `Step` Protocol (`name="eda"`, `requires=(("data","frame"),("data","labels"))`, `provides=` los 5 artefactos `"eda"`); `execute(study, rng)` lee `artifacts["data"]`, filtra por `cfg.analysis_partition`, orquesta default_rate→stability→univariate→quality→figuras, publica `study.artifacts.set("eda", k, v)`. **NO muta `artifacts["data"]` ni el frame.** `FigureSpec` declarativa (receta+datos, NO renderiza; matplotlib/plotly NO se importan aquí). En `core/study.py`: extender `_ensure_domain_registered`/`_coerce_domain_config` (hoy `if name=="data"`) para `"eda"` (import perezoso, **generaliza el mapeo dominio→módulo, salda deuda HANDOFF**); `_default_step_names` añade `"eda"` tras `"data"` si la sección está activa. Núcleo liviano intacto. SDD-27 §2/§4/§6/§7/§10 + CT-1. + `tests/unit/test_eda_step.py` + `tests/integration/test_eda_pipeline.py` (`study.run(steps=["data","eda"])` puebla los 5 artefactos; frame idéntico antes/después; `assert_bitwise_reproducible` sin muestreo).
  - [x] **B5.7** ✅ (`eaeb78e`, CI verde run `28272040338`, revisor✓ a la 1ª) — `eda/card.py` (`EdaCardSection`, `BaseModel` con `arbitrary_types_allowed`): resumen auditable del EDA para model card / report (patrón `data/card.py`) — `overall_rate`, `n_periods`, `stability_flagged`+`metric_used`+`threshold`+`valor`, `n_columns_profiled`, recuento de flags de calidad. Construido en `EdaStep`, publicado como `("eda","eda_card")`; SDD-26 lo lee. **Solo resumen, no recalcula.** SDD-27 §1/§4. + `tests/unit/test_eda_card.py` (golden bit-idéntico de campos resumidos desde un `EdaResult` conocido). _(Aditivo: si Cami prefiere que report ensamble el resumen desde los 5 artefactos, este es el ítem recortable.)_
  - **ORDEN:** B5.1→B5.2→B5.3(consume `DefaultRateResult`)→B5.4→B5.5→B5.6(integra B5.2–B5.5)→B5.7(consume `EdaResult`). B5.2–B5.5 son analizadores independientes; el orden lineal mantiene un-trozo/commit con árbol verde.
  - **NOTAS DE DISEÑO (para revisión final de Cami):** (1) **PSI corregido**: SDD-27 §3+D-EDA-1 son explícitos en que el PSI es SDD-11 (sobre el *score*), NO `eda` — el troceo sugerido del HANDOFF lo mezclaba. (2) **B5.1 y B5.6 tocan `core/`** (no solo `eda/`): cablear la sección `eda` en `NikodymConfig` + extender el registro de dominios de `Study` (hoy hardcodea `data`); re-verificar el gate de núcleo liviano tras ambos. (3) **`eda ∉ INFRA_SECTIONS`** (SDD-05 §5.5): a diferencia de audit/governance/tracking, `EdaConfig` SÍ entra al `config_hash`. (4) **Fronteras resolubles con el default del SDD (no bloquean, marcadas):** contrato `FigureSpec`↔SDD-26 (report aún Pendiente; se fija con §4 y se confirma al diseñar report); accessor de particiones SDD-02 (D-EDA-2, hoy se filtra por la columna `partition`); nombre del extra de gráficos (se difiere a SDD-25/26, `eda` no importa matplotlib/plotly).
- [x] **A2 — B4.5 Python 3.13 ✅ HECHO** (`1c9623b`, CI verde run `28273170069` con jobs 3.13 ubuntu+macos, revisor✓). Constraint uv `ortools>=9.12` bajo `[tool.uv]` (primer minor con wheel cp313 — verificado que 9.11.x NO lo tiene; NO override, NO dep directa) → lock recomputado `ortools 9.10.4067→9.15.6755`. Matriz del job `test` del CI +3.13 (`[ubuntu,macos] × [3.11,3.12,3.13]`) + comentario de desviación actualizado. Verificado en **Python 3.13 real local** (`uv sync --locked --python 3.13 --extra scoring` + pytest → 629 passed), no solo que el lock resolviera. **🔴 EFECTO COLATERAL (deuda para A5/F1 binning):** el constraint arrastra **optbinning 0.21.0→0.20.0** (0.21 fija `ortools<9.12`); benigno hoy (nada ejerce binning; 0.20 cumple el floor `>=0.19`) pero **revisar la API optbinning 0.20 vs 0.21 al implementar SDD-06**. (databricks-sdk 0.119→0.72 dev-only vía mlflow, fuera de runtime, benigno.) **DECISIÓN AUTÓNOMA para Cami:** piso `>=9.12` en vez del `>=9.11` literal del backlog (9.11 no publica wheel cp313).
- [x] **A3 — B4.4 Windows ✅ HECHO** (`9863060`, CI verde run `28274334345` con los 3 jobs windows-latest × 3.11/3.12/3.13 SUCCESS, revisor✓). `Study.save` Windows-safe: el swap atómico apartaba el respaldo con `os.replace` sobre un dir creado por `mkdtemp` (destino existente) → `PermissionError [WinError 5]` en Windows. Fix AGNÓSTICO al SO (sin ramas `sys.platform`): el respaldo lateral se reserva como ruta **inexistente** (`_missing_backup_path` = `mkdtemp`+`rmdir`) y todos los renombrados pasan por `_replace_path` con retry acotado (3×, backoff 50ms) ante locks transitorios del FS (antivirus/indexador). El doble-fallo (swap + restauración) **preserva** el estudio previo en el respaldo lateral en vez de borrarlo. Matriz del CI +`windows-latest` (3 SO × 3 Python); comentario de desviación SDD-25 §7 actualizado. Tests agnósticos (monkeypatch de `os.replace`/`time.sleep`) que cubren respaldo-inexistente, retry-transitorio y retry-agotado-limpia-tmp. Otros usos de `os.replace`/temporales (mixins/recorder/sink) revisados: no comparten el patrón de directorios. **🔴 DECISIÓN AUTÓNOMA (para revisión de Cami):** (1) `_replace_path` solo reintenta `PermissionError` (es lo que mapea `WinError 5`); permisos realmente denegados re-lanzan tras ~0.1s, no se enmascaran. (2) Doble-fallo preserva el estudio previo (más seguro que borrarlo), pero el docstring de `save` aún dice "nunca hay un instante sin un directorio válido en `path`" → sobre-promete ese caso extremo (nitpick F1, matizar el docstring).
- [ ] **A4 — Deudas menores F1 (limpieza segura, sin decisión de producto).** ⚠ **(a)-(d) BLOQUEADAS para push hasta desbloquear billing del CI (R0).** Un trozo/commit cada una: **(0) ✅ HECHO (`d932f52`, 2026-06-26 21:53) — cobertura `eda/univariate.py:240->263`** (rama `if not non_missing.empty:` falsa = columna **categórica completamente faltante**) → restaurada **99.98%→100%** con 1 test de columna categórica dtype string 100% NaN; revisor✓; gates locales 100% verdes (verificado esta corrida: 633 tests, 0 miss). _Su CI quedó rojo POR BILLING (run `28275055048`), no por código._ (a) **B4.1** — quitar el test tautológico `test_golden_seed_sequence_equivale_a_seed_manager`; `REGISTRY.unregister` del Step dummy de `dummy_step_config` (cleanup); documentar el caveat de orden de `pickle` en `reproducibility._canonical_bytes`. (b) **B3.1c** — alinear/decidir métricas-como-tags `nikodym.metric.*` (vs SDD-04 §4) + DRY `_registry_db_backed` (`tracking/inventory.py`+`recorder.py`). (c) **CI** — subir `actions/checkout`/`astral-sh/setup-uv` a versiones sobre Node 24 si ya salieron; revisar si conviene acotar la matriz `test` 3×3 en ramas no-`main`/no-PR (SDD-25 §7). (d) **nitpicks** FIX-hashing (docstring §7 `sort_index`; assert `category` con distinto orden de categories) + FIX-target (comentario regulatorio en mask NaT; doc filas disjuntas en test de acumulación) + B4.4 (matizar docstring de `Study.save` para el caso doble-fallo).
- [x] **A4 — (ENTRADA DUPLICADA — consolidada en la entrada A4 de arriba, que ya incluye (0)-(d); NO tomar, se conserva solo como referencia histórica).** Un trozo/commit cada una: (a) **B4.1** — quitar el test tautológico `test_golden_seed_sequence_equivale_a_seed_manager`; `REGISTRY.unregister` del Step dummy de `dummy_step_config` (cleanup); documentar el caveat de orden de `pickle` en `reproducibility._canonical_bytes`. (b) **B3.1c** — alinear/decidir métricas-como-tags `nikodym.metric.*` (vs SDD-04 §4) + DRY `_registry_db_backed` (`tracking/inventory.py`+`recorder.py`). (c) **CI** — subir `actions/checkout`/`astral-sh/setup-uv` a versiones sobre Node 24 si ya salieron. (d) **nitpicks** FIX-hashing (docstring §7 `sort_index`; assert `category` con distinto orden de categories) + FIX-target (comentario regulatorio en mask NaT; doc filas disjuntas en test de acumulación).
- [ ] **A5 — Continuación F1 DISEÑA-Y-APLICA (mandato Cami 2026-06-26).** Cuando A1–A4 se agoten, avanzar el pipeline de scoring en orden canónico: **binning (SDD-06) → selection (07) → model (08) → scorecard (09) → calibration (10) → performance+stability (11) → report (26)**. Procedimiento por fase: (i) un worker **REDACTA el SDD-0X** (Borrador) anclado a la normativa CMF + ESPECIFICACIONES + SDDs aprobados vecinos, con el formato de los SDD existentes y una sección **"Decisiones para revisión de Cami"**; (ii) marcar el SDD ✅ en `00-INDICE.md`; (iii) el planificador lo trocea; (iv) implementar un-módulo/commit con tests 100% + revisor. **La revisión de DISEÑO la hace Cami AL FINAL.** Si una fase no se puede diseñar con seguridad desde la normativa, márcala bloqueada y pasa a la siguiente.

1. [x] **B2b.1** — `data/loading.py` (`DataLoader`): CSV/Parquet + passthrough de DataFrame con copia defensiva, `engine="pyarrow"` explícito, `backend="polars"` perezoso, `from_config`. SDD-02 §4. + `tests/unit/test_data_loading.py`. ✓ `6d699e2` (266 tests, 100%).
2. [x] **B2b.2** — `data/schema.py` (`SchemaValidator`): `SchemaConfig`→`pa.DataFrameSchema` (`import pandera.pandas as pa`), `validate(df, lazy=True)`→`DataValidationError` (reporte español). SDD-02 §7. ✓ `bdbea17` (282 tests, 100%). _Nota ratificada: `index_col`=nombre del índice existente (no `set_index`); `SchemaValidator` no exportado en `data.__init__`._
3. [x] **B2b.3** — `data/hashing.py` (`data_hash`): sha256 por bloques (`hash_pandas_object`), **endianness `<u8` explícito**, `-0.0→0.0`, `index=True`, defaults `hash_key`/`encoding`, golden cross-versión. SDD-02. ✓ `c7edaca` (293 tests, 100%). _Ratificado: ordena por índice (`sort_index` estable) → invariante a permutación de filas; eleva O(n) §7 a O(n log n), aceptado. Header de esquema versionado `nikodym.data_hash.v1`. No exportado en `data.__init__`._
4. [x] **B2b.4** — `data/special.py` (`SpecialValuePolicy`): centinelas→NaN + `special_mask` + `special_catalog`. SDD-02. ✓ `cf2487a` (302 tests, 100%). _Detección type-safe por dtype (evita FutureWarning con `filterwarnings=error`); `label` de `SpecialValueSpec` NO se propaga al `MaskedFrame` (catálogo = columna→centinelas, fiel a §4) → binning/SDD-06 releerá la config. No exportado en `data.__init__`._
5. [x] **B2c.1** — `data/target.py` (`TargetDefinition`): mini-DSL declarativo (allowlist cerrada de operadores, sin `eval`) → `target` `Int8` 1/0/NA + `label_status` categórico; precedencia exclusión>indeterminado>malo>bueno; ventana de desempeño no madurada→`excluido(ventana_incompleta)`. SDD-02 §4/§7. ✓ `3ea9523` (332 tests, 100%). _Consume `TargetConfig`/`Predicate`/`Rule`/`ExclusionRule`/`PerformanceWindow` ya existentes (B2a); valida compatibilidad dtype↔valor ANTES de comparar (lección B2b.4); `LabeledFrame`/`TargetDefinition` NO exportados en `data.__init__` (se cablea en B2d)._
6. [x] **B2c.2** — `data/partition.py` (`Partitioner`: temporal/random/cohort) con **Hypothesis** para determinismo y anti-leakage. SDD-02. ✓ `2e461c6` (359 tests, 100%). _Identidad de partición estable por `blake2b(root_seed:index)` (`byteorder="little"`, `-0.0→0.0`) → invariante a permutación e inserción, sin depender del orden posicional ni del `Generator` (`del rng`). OOT por corte (temporal/cohort) o fracción (random); HO por umbral de hash; rol `ttd` configurable. `Partition`/`PartitionResult`/`Partitioner` con `__all__` propio, NO reexportados en `data.__init__` (se cablea en B2d)._
7. [x] **B2d** — `data/step.py` (`DataStep @register("standard", domain="data")`) + `Study.run(steps=["data"])` end-to-end de datos. SDD-02 §4/§7 + CT-1. ✓ `1b01f6a` (370 tests, 100%). _Cableado vía `REGISTRY` con **import perezoso** de `nikodym.data` dentro de `Study._resolve_step` (núcleo liviano intacto, gate verde); reexports completos en `data/__init__.py`; nuevo `data/card.py` (`DataCardSection`) para el artefacto `data_card`; inyección de DataFrame en memoria por `("data","input_frame")` para correr sin I/O. **B2 COMPLETO.**_
8. **B3.1** — integración `audit`/`governance`/`tracking`/`api` (SDD-03/04, CT-4). Troceado un-módulo/commit:
   - [x] **B3.1a** — `audit/` (`JsonlAuditSink` JSONL append-only + `EnvironmentSnapshot`/`capture_environment` + `hashing` + `replay` + `AuditConfig` INFRA + `AuditError`) + cableado `NikodymConfig.audit` (hook diferido `_AUDIT_CONFIG_CLS`). SDD-03 §4.1/§5. ✓ `eb62d45` (387 tests, 100%).
   - [x] **B3.1b** — `governance/` (`ModelCard`/`DecisionRecord`/`ModelCardBuilder`, `ModelInventory` Protocol `@runtime_checkable`, `InventoryEntry`/`InventoryRecord`, `ScenarioLog`/`OverlayRecord`, `GovernanceConfig`) + cableado `NikodymConfig.governance`. SDD-03 §4.2. ✓ `8d4e543` (423 tests, 100%). _Cableado idéntico al patrón `audit`/`data` (hook diferido `_GOVERNANCE_CONFIG_CLS` + `field_validator` + campo `Any`-runtime/`GovernanceConfig`-`TYPE_CHECKING`). `governance` es sección **INFRA** (excluida de `config_hash`). `ModelCardBuilder` con `now`/`environment_provider` inyectables → golden bit-idéntico; `_add_months` stdlib (evita pandas, refuerza núcleo liviano). `ModelInventory` = solo Protocol + tipos (impl MLflow → B3.1c). No exportado en `core`._
   - [x] **B3.1c** — `tracking/` (`TrackingRecorder`/`TrackingSink`/`MLflowInventory`) + `api.assemble_run` (compone `FanOutSink`, resuelve inventario; CT-4) + `NikodymConfig.tracking`. SDD-04. ✓ `731f099` (456 tests, 100%). _MLflow import perezoso (núcleo liviano ampliado con `mlflow`/`nikodym.tracking`); `autolog=False`, aliases no stages, `fail_on_tracking_error` degrada a warning+no-op; idempotencia `register` por `(model_name,config_hash)`; `MLflowInventory` satisface el Protocol `ModelInventory` (B3.1b); `assemble_run` resuelve `NullInventory`/`MLflowInventory` por `governance.publish_to_inventory` (ya existía, B3.1b) con `MissingDependencyError` ruidoso; tests con fakes deterministas (mlflow NO instalado). **B3 COMPLETO.** 🟡 deuda: métricas duplicadas como tags `nikodym.metric.*` vs nota SDD-04 §4 (benigno, ver Decisiones)._
9. **B4** — `testing` + CI + 3 criterios de cierre del Hito 0. Último bloque de F0. Troceado un-trozo/commit (precedente B3.1):
   - [x] **B4.1** — `src/nikodym/testing/` (helpers públicos, SDD-24): `estimator_checks` (`check_nikodym_estimator` + 9 checks + `all_nikodym_checks`), `strategies` (`nikodym_config_strategy` + `discriminated_union_tags`; **Hypothesis import perezoso**), `fixtures` (`minimal_study`/`dummy_step_config`/`golden_seed_sequence`), `reproducibility` (`assert_bitwise_reproducible`) + gate liviano ampliado con `hypothesis`. ✓ `65a9204` (510 tests, 100%). _Hypothesis NO se expone como extra público (SDD-24): queda como dependency-group dev/test + import perezoso con mensaje. `dummy_step_config` registra Step dummy idempotente en REGISTRY global (no se limpia; benigno en F0). Deuda menor → F1 (ver Decisiones B4.1)._
   - [x] **B4.2** — Los **3 criterios de cierre del Hito 0** (`_CONTRATOS-TRANSVERSALES.md` §4) en `tests/unit/test_hito0_contracts.py`: (1) Step dummy con **fan-in** (binning+calibration→ifrs9; positivo corre / negativo falla en **validación pre-run** con `ConfigError ...inejecutable`); (2) **payload estructurado** ECL-like por `log_metrics`/`ModelCard` + `payload` no escalar por `OverlayRecord` (golden JSON + snapshot profundo + round-trip JSONL); (3) **gate regulatorio sin falsos verdes** (nuevo `testing/regulatory.py` con fuente única + verificación de existencia). + endureció governance con `deepcopy` (cierra deuda B3.1b). ✓ `1c856f5` (516 tests, 100%).
   - [x] **B4.3** — CI: `.github/workflows/ci.yml` (7 jobs: `quality` ruff+mypy · `test` matriz **ubuntu+macos × 3.11/3.12** · `test-all` --all-extras · `coverage-regulatory` deriva el `--include` de `nikodym.testing.regulatory` · `build` con smoke + license-expression + anti-copyleft + license-report artifact · `lock-check` · `release` por tag `v*`/OIDC) + perfiles Hypothesis `dev`/`ci`/`nikodym_deterministic` centralizados en `tests/conftest.py`. ✓ `6e55be8` (516 tests, 100%; **CI VERDE en GitHub run `28156768899`, F0 CERRADO**). _v2 tras revertir el 1er intento `11ea69a` (CI rojo): anti-copyleft robusto (solo bloquea copyleft real GPL/LGPL/AGPL; nvidia/propietarias/metadata-incompleta pasan con warning), hash-EOL `write_bytes`, matriz acotada (Windows→B4.4 / 3.13→B4.5, desviación SDD-25 §7 documentada en el yaml); bump `numba>=0.60`/`llvmlite>=0.43` del extra `explain`._
   - [ ] **B4.4 (⚠ DECISIÓN CAMI = R0: ¿soportar Windows en v0.1.0?)** — portabilidad Windows: `Study.save` (`core/study.py:372`) lanza `PermissionError [WinError 5]` en el swap atómico (rename sobre destino existente/bloqueado) → hacerlo Windows-safe; + revisar otros usos de EOL/paths. NO reproducible localmente (necesita runner Windows). Al cerrarlo, añadir `windows-latest` a la matriz del CI. _(El hash-EOL ya se mitigó en B4.3 v2 con `write_bytes`.)_
   - [ ] **B4.5 (⚠ DECISIÓN CAMI = R0: ¿soportar Python 3.13 en v0.1.0?)** — `ortools` (vía optbinning/scoring) no tiene wheel cp313 en 9.10.4067. Subir ortools a ≥9.11 (verificar techo de optbinning y cascada del lock) o dejar 3.13 fuera del job con `--extra scoring`. Al cerrarlo, añadir `3.13` a la matriz del CI.

## 🔧 Backlog de FIXES (revisión adversarial independiente, 2026-06-25) — PRIORITARIO antes de F1
> El **revisor del paso 6.5** (subagente Claude adversarial, motor cruzado) re-revisó los módulos de la noche y halló
> bugs **MATERIALES** que los 4 gates + 100% cobertura + la revisión del maestro nocturno NO vieron. Arreglar ANTES
> de avanzar a F1. El revisor del ciclo los valida en cada push (ya probado E2E con FIX-partition).
10. [x] **FIX-hashing** — `data/hashing.py` `data_hash` ahora **rechaza índice duplicado** con `DataValidationError` (`_validate_unique_index` fail-fast antes del hash). + test índice duplicado + test `category` (golden cross-OS). SDD-02 §6/§7. ✓ `019ca1b` (520 tests, 100%; revisor✓; CI verde). _Decisión (R11): **fail-fast** > "invariante-a-permutación" — reordenar los `uint64` habría cambiado TODOS los golden y forzado header `v2`; fail-fast preserva el algoritmo y los golden `v1` para índice único, y es consistente con FIX-partition (misma excepción `core.exceptions.DataValidationError`). Nitpicks del revisor (deuda menor F1, NO bloqueantes): (1) el docstring de módulo de `hashing.py` aún justifica `sort_index` por "invariancia a permutación de filas" — solo cierto para índice único ahora; aclarar media línea. (2) añadir al test `category` un assert que compare hashes con distinto orden de `categories` para blindar el golden contra regresiones de pandas._
11. [x] **FIX-target** — `data/target.py`: (a) `NaT` en `observation_date`/`cutoff` se trataba como ventana madurada → etiqueta **bueno** (censura insegura) → ahora `mask = (matured>cutoff) | obs.isna() | cutoff.isna()` (+`fillna(False).astype("bool")`) → `excluido(ventana_incompleta)`; (b) `exclusions_by_reason[reason]=count` **sobrescribía** (subcuenta si 2 máscaras comparten razón → cifra regulatoria errada en el model card) → ahora `get(reason,0)+count` + `model_validator` en `TargetConfig` que rechaza nombres de exclusión duplicados y la colisión con `EXCLUSION_WINDOW_REASON="ventana_incompleta"`. SDD-02 §3/§12. ✓ `f552c72` (526 tests, 100%; revisor✓; CI verde run `28189548182`). _Constante `EXCLUSION_WINDOW_REASON` movida `target.py`→`config.py` (config es la capa base; evita import circular, núcleo liviano intacto). +6 tests TDD (3 casos NaT + control golden, duplicados/colisión→`ValidationError`, consistencia config↔target, acumulación vía `model_construct` para forzar 2 fuentes con misma razón). Nitpicks del revisor (deuda menor F1, NO bloqueantes): (1) comentario de intención regulatoria en la máscara NaT de `target.py`; (2) documentar que el validador detecta duplicados en orden de declaración; (3) comentar en el test de acumulación que las filas son disjuntas a propósito (si se solaparan, el `& ~excluded_mask` daría conteo 1)._
12. [x] **FIX-partition (B2c.2)** — índice duplicado → `DataValidationError` tipado + cero-buenos → guard simétrico. ✓ **(E2E supervisada: worker Codex → gates → revisor RECHAZÓ una regresión → re-trabajo → revisor APROBÓ → push).** _`stratify_by` exacto = R0-stratify, abajo._

> **R0 (decisión de Cami) — estado tras las aprobaciones del 2026-06-26:**
> - **R0-stratify** — _ABIERTA, no urgente._ `partition.py` `stratify_by`: estratificación **EXACTA** (preservar bad-rate en Dev/HO/OOT) vs **insert-stability** documentada están en **tensión matemática**. Quedó **revertido a umbral-global insert-stable** (no-op; nota en `partition.py:286`). No bloquea EDA ni F1 inmediato; Cami decide al volver qué garantía prioriza el path estratificado.
> - **R0-A** — ✅ **DELEGADA (2026-06-26):** Cami autorizó que el sistema **diseñe SDD-06…11, 26 y los aplique** (mandato diseña-y-aplica, ítem **A5**); la revisión de diseño la hace Cami al final.
> - **R0-B** — ✅ **APROBADA (2026-06-26):** **SDD-27 (eda)** marcado Aprobado en `00-INDICE.md` → EDA = ítem **A1**.
> - **R0-C** — ⛔ **SIGUE SIENDO SOLO DE CAMI (irreversible):** Release público **v0.1.0** / repo público / PyPI. **NO ejecutar en autónomo.**
> - **R0-D** — ✅ **APROBADA (2026-06-26):** **B4.4** Windows (ítem **A3**) y **B4.5** Python 3.13 (ítem **A2**).

## Estado actual
**Nikodym RiskLib — F0 (Fundación) ✅ CERRADO: B1 `core` ✅ + B2 `data` ✅ + B3 (audit/governance/tracking/api) ✅ + B4 ✅ (B4.1 `testing`, B4.2 criterios Hito 0, B4.3 CI verde en GitHub).** El CI corre verde en **ubuntu+macos+windows × 3.11/3.12/3.13** (B4.5/A2 + B4.4/A3 cerrados → **matriz de plataformas COMPLETA**, desviación SDD-25 §7 cerrada). **+ "🔧 Backlog de FIXES" (revisión adversarial) ✅ CERRADO**: FIX-partition (`baac2a9`), FIX-hashing (`019ca1b`), FIX-target (`f552c72`). **+ A1 B5 EDA ✅ COMPLETA (7/7) + A2 B4.5 Py3.13 ✅ (`1c9623b`) + A3 B4.4 Windows ✅ (`9863060`)**. Cola autónoma en curso: **A4 deudas F1 → A5 diseña-y-aplica F1 (binning..report)**. Deuda de alcance R0 restante: solo **R0-C release público v0.1.0** (SOLO de Cami; ya NO queda deuda de plataforma). 🟡 Deuda de cobertura nueva (no-R0): branch `eda/univariate.py:240->263` sin cubrir → 99.98% global → A4(0).

**B3 por dentro (ítem 8 del backlog, troceado un-módulo/commit) — COMPLETO:** **B3.1a ✅** (`audit/`: `JsonlAuditSink` + `EnvironmentSnapshot` + `hashing` + `replay` + `AuditConfig` INFRA + cableado `NikodymConfig.audit`, `eb62d45`) · **B3.1b ✅** (`governance/`: `ModelCard`/`ModelCardBuilder` + `ModelInventory` Protocol + `InventoryEntry`/`InventoryRecord` + `ScenarioLog`/`OverlayRecord` + `GovernanceConfig` INFRA + cableado `NikodymConfig.governance`, `8d4e543`) · **B3.1c ✅** (`tracking/` MLflow: `TrackingRecorder`/`TrackingSink`/`MLflowInventory` + `api.assemble_run` `FanOutSink` CT-4 + cableado `NikodymConfig.tracking` INFRA, `731f099`). _(B2: B2a config · B2b loading/schema/hashing/special · B2c target/partition · B2d step+cableado, todo ✅.)_

Regla de oro vigente: **mixto-troncal-más-incremental** — cada módulo: programa → `ruff`+`mypy --strict`+`pytest`+cobertura verde → ajusta → sigue. Nunca avanzar en rojo. Reabrir un SDD por feedback del código es esperado y barato. **Nuevo (B4.3): el CI en GitHub es ahora parte del verde** → tras pushear, esperar `gh run view` del run verde antes de cerrar.

## Hecho en la última corrida (A3 / B4.4 — `Study.save` Windows-safe — ✓ HECHO, CI VERDE)
**Resultado:** **Nikodym ya es portable a Windows** y el CI lo cubre: `Study.save` (persistencia atómica por directorio) funciona en `windows-latest` × 3.11/3.12/3.13 (verificado en runner Windows REAL, no reproducible en local). Queda **cerrada la desviación de SDD-25 §7** (la matriz de plataformas ya cubre Linux+macOS+Windows × 3 Python). Tarea de portabilidad + CI (no un módulo nuevo).
- **El bug:** el swap atómico apartaba el directorio destino con `os.replace(destino, respaldo)` donde `respaldo = mkdtemp(...)` **crea** el dir → en Windows renombrar un dir sobre un destino **existente** lanza `PermissionError [WinError 5]` (POSIX lo permite si el destino está vacío; Windows no). 
- **El fix (AGNÓSTICO al SO, sin ramas `sys.platform`):** (1) `_missing_backup_path(destino)` reserva una ruta de respaldo **única e inexistente** (`mkdtemp`+`rmdir`) → `os.replace` hacia una ruta que no existe funciona en Windows y POSIX; (2) `_replace_path(src,dst)` envuelve los `os.replace` con **retry acotado** (`_REPLACE_RETRY_ATTEMPTS=3`, backoff `0.05s`) que solo captura `PermissionError`, para tolerar locks transitorios del FS (antivirus/indexador) sin enmascarar errores persistentes; (3) en el `except` exterior se eliminó el `shutil.rmtree(respaldo)` → en el **doble-fallo** (swap falla Y la restauración falla) el estudio previo se **preserva** en el respaldo lateral `.<name>.old.*` en vez de destruirse (estrictamente más seguro que antes). Matriz del CI `test`: +`windows-latest`; comentario de desviación SDD-25 §7 actualizado.
- **🔴 DECISIONES AUTÓNOMAS (para revisión final de Cami):** (1) retry restringido a `PermissionError` (lo que mapea `WinError 5`); un permiso realmente denegado re-lanza tras ~0.1s. (2) doble-fallo preserva en vez de borrar el estudio previo — el docstring de `save` aún promete "nunca hay un instante sin un directorio válido en `path`", que sobre-promete ese caso extremo (nitpick → A4(d), matizar el docstring; el comportamiento real es más seguro que el documentado).
- **Tests (sin runner Windows → agnósticos vía monkeypatch):** `os.replace`/`time.sleep` parcheados para ejercer en Linux las ramas que importan en Windows — respaldo-inexistente (assert de que la ruta `.old.` NO existe al renombrar), retry-transitorio (`PermissionError` 1ª vez → éxito 2ª, `calls==2`), retry-agotado (lock persistente → re-lanza tras 3 intentos, sin destino parcial ni temporales). `test_save_sobre_existente_reescribe` reforzado (verifica `load(...).config.name == "segundo"`, no tautológico). Golden reales.
- **Gates locales (maestro, R7): verdes** — ruff + format(119) + mypy(69) + **632 tests** (+3) + **`core/study.py` 100%** (234 stmts, 0 miss, 68 branch, 0 BrPart) + regulatorio 100% + núcleo liviano + `uv build`. ⚠ **Cobertura GLOBAL 99.98%, NO 100%:** 1 branch parcial en `eda/univariate.py:240->263` (columna categórica 100% faltante) — **AJENO a A3** (no toca univariate; los tests de A3 son solo de `study.py`), **preexistente** (probablemente expuesto por el cambio de deps de A2), determinista en ambos perfiles Hypothesis (dev y ci). El CI no lo gate-ea (`test` con `--fail-under=90`; el job regulatorio solo mira los 4 módulos críticos, 100%). Registrado como **A4(0) prioritaria** (fix = 1 test de columna categórica todo-NaN). No bloquea A3.
- **Worker Codex:** reciclado fresco; construcción ~5.5 min, gates verdes a la 1ª (sin ciclos de re-trabajo). Iba a invocar `cierre-trabajo` (que toca HANDOFF/docs) → **nudge del maestro** lo acotó a NO tocar markdown; respetó el lock RUNNING del HANDOFF.
- **Revisor adversarial (Claude, motor cruzado vs Codex): APROBADO.** MATERIAL: ninguno. Verificó los 5 caminos del swap (éxito; sobrescritura; fallo+restauración; doble-fallo; fallo en escritura del tmp), que el doble-fallo preserva (no pierde) datos, que el retry no enmascara errores no-transitorios, que los tests son agnósticos al SO y no tautológicos, y que los `run:` del job `test` corren en PowerShell sin `shell: bash`. Nitpicks (no materiales → A4): docstring del doble-fallo sobre-promete; race teórica despreciable en `_missing_backup_path`.
- **Push `9863060`; CI en GitHub VERDE** (run `28274334345`, **14 jobs success** incl. los **3 nuevos `Tests (windows-latest, Python 3.11/3.12/3.13)`** + ubuntu×3 + macos×3 + Tests-all-extras + Quality + Build + Regulatory + Lock-check; Release skipped; verifiqué CADA job individual, sin fiarme del agregado).

## Histórico — A2 / B4.5 (`1c9623b` · soporta Python 3.13 — ✓ HECHO, CI VERDE)
**Resultado:** Nikodym ya **resuelve, instala y pasa los tests en Python 3.13**, y el CI lo cubre. Tarea de **dependencias + CI** (no un módulo). `ortools` llegaba como **transitiva de `optbinning`** (extra `scoring`) pineada en `9.10.4067`, que **no publica wheel cp313** → único bloqueo para 3.13. Fix: `constraint-dependencies = ["ortools>=9.12"]` bajo `[tool.uv]` (NO override, NO dep directa; `9.12` es el primer minor con wheel cp313 — verificado en PyPI que `9.11.x` no lo tiene) → `uv.lock` recomputado, **ortools 9.10.4067 → 9.15.6755**. Matriz del job `test` del CI: **+3.13** (`[ubuntu,macos] × [3.11,3.12,3.13]`); comentario de desviación actualizado (solo queda Windows/B4.4). Archivos: `pyproject.toml`, `uv.lock`, `.github/workflows/ci.yml`.
- **🔴 DECISIÓN AUTÓNOMA (para revisión final de Cami):** piso `ortools>=9.12` en vez del `>=9.11` literal del backlog, porque ortools 9.11.x no publica wheel cp313 (con `>=9.11` uv habría saltado igual a ≥9.12 para conseguir el wheel). Constraint, no override: si una versión upstream fija un techo, uv elige otra versión compatible en vez de reemplazar metadata de terceros.
- **🔴 EFECTO COLATERAL → DEUDA para A5/F1 (binning, SDD-06):** el constraint `ortools>=9.12` **entra en conflicto con optbinning 0.21.0** (que fija `ortools<9.12,>=9.4`), y uv lo resuelve **degradando optbinning 0.21.0 → 0.20.0 en TODOS los Python** (incl. 3.11/3.12 que antes corrían 0.21.0). **Benigno hoy** (nada en el código ejerce binning aún; 0.20.0 cumple el floor propio `optbinning>=0.19`; lock consistente y reproducible) **PERO al implementar SDD-06/binning (A5) hay que revisar la API de optbinning 0.20 vs 0.21**, y reconsiderar si en el futuro conviene quedarse en `ortools<9.12` (entonces Python 3.13 dependería de que optbinning publique una versión con `ortools>=9.12`). Hallazgo del **revisor adversarial**, no de los gates. (databricks-sdk 0.119→0.72 también bajó, dev-only vía mlflow, fuera de runtime/scoring → benigno.)
- **Gates locales (maestro, R7): verdes** — ruff + format(119) + mypy(69) + **629 tests** + cobertura **100% global** (3727 stmts, 0 miss) + regulatorio 100% + núcleo liviano + `uv build` + `uv lock --check`.
- **Verificación en Python 3.13 REAL (no solo el lock):** `uv python install 3.13` + `uv sync --locked --python 3.13 --extra scoring` + `pytest -q` → **629 passed** (reproduje el job 3.13 localmente antes de pushear — ventaja sobre B4.4 Windows, que no es reproducible local).
- **Worker Codex:** reciclado fresco; ~7.5 min, gates verdes a la 1ª (sin ciclos de re-trabajo).
- **Revisor adversarial (Claude, motor cruzado vs Codex): APROBADO.** MATERIAL: ninguno. Verificó con dry-run que 3.13 resuelve **wheels-only** (sin sdist) en toda la matriz, que `>=9.12` es el piso correcto y que ningún test ejerce ortools/optbinning hoy → reproducibilidad intacta. El downgrade de optbinning es el único NITPICK (anotado arriba como deuda F1).
- **Push `1c9623b`; CI en GitHub VERDE** (run `28273170069`, **11 jobs success** incl. los nuevos **Tests ubuntu-3.13** y **macos-3.13** + 3.11/3.12 ambos OS + Tests-all-extras + Quality + Build + Regulatory + Lock-check; Release skipped; verifiqué cada job individual sin fiarme del agregado).

## Histórico — B5.7 (`eaeb78e` · `eda/card.py` `EdaCardSection` — ✓ HECHO, CI VERDE)
**Resultado:** **A1 B5 EDA ✅ COMPLETA (7/7).** Existe el **resumen auditable del EDA** para el model card / report (SDD-26). `EdaCardSection` (`eda/card.py`, `BaseModel` con `arbitrary_types_allowed`, patrón `data/card.py`) con 9 campos: `overall_default_rate`, `n_periods`, `stability_flagged`/`stability_metric_used`/`stability_threshold`/`stability_value`, `n_columns_profiled`, `quality_flag_counts` (dict), `n_figures`. Se construye en `EdaStep._build_eda_card(result=...)` **leyendo SOLO los campos ya calculados del `EdaResult`** (`len(by_period)`, `float(getattr(stability, metric_used))`, `int(by_column[flag].sum())` sobre los 3 flags booleanos `near_constant`/`near_unique`/`high_cardinality`, `len(profiles)`, `len(figures)`) — **NO recalcula** analizadores ni muta `by_column`/`profiles`. Se publica como **6º artefacto** `("eda","eda_card")` (`EDA_ARTIFACTS` y los docstrings pasan de "cinco" a "seis"); `eda_card` es artefacto SEPARADO, NO un campo de `EdaResult` (espejo de `data_card`). Reexport perezoso `EdaCardSection` en `eda/__init__.py`. SDD-27 §1/§4 + nueva D-EDA-5 (card como artefacto separado).
- **🔴 DECISIÓN AUTÓNOMA (frontera, para revisión final de Cami):** los nombres exactos de los campos del card y que "recuento de flags de calidad" = `quality_flag_counts: dict[str,int]` sobre los 3 flags booleanos (excluye `missing_rate`, que es continuo). Documentada en el docstring de `card.py` y en el SDD-27. Si Cami quiere además contar columnas con missing>0 o un total agregado, es un ajuste de un campo.
- **Gates locales (maestro, R7): verdes** — ruff + format(119) + mypy(69 source files) + **629 tests** (+3 vs B5.6) + cobertura **100% global** (3727 stmts, 0 miss) + regulatorio 100% + núcleo liviano + `uv build`.
- **Worker Codex:** reciclado fresco; construcción ~5.5 min, gates verdes a la 1ª (sin ciclos de re-trabajo).
- **Revisor adversarial (Claude Opus, motor cruzado vs Codex): APROBADO A LA 1ª.** MATERIAL: ninguno. Verificó que el card SOLO lee (no recalcula/no muta), que la propagación de NaN (`overall_rate`/`cv` sin períodos elegibles) es comportamiento **documentado** en SDD-27 §8 y que los analizadores ya evitan `flagged=True` espurio (`stability.py` usa `isfinite`), que los golden NO son tautológicos, y que ningún test quedó esperando 5 artefactos. **3 nitpicks NO materiales → deuda menor F1:** (1) falta golden de `metric_used="trend_slope"` (el único negativo) en `test_eda_card.py` — el path del card es metric-agnóstico (`getattr`), cubierto al 100%; (2) `quality_flag_counts` asume el contrato fijo de `QualityResult.by_column` (hoy seguro; si otro productor lo poblara, validar con pandera); (3) NaN propagado registrado solo por transparencia (no es hallazgo).
- **Push `eaeb78e`; CI en GitHub VERDE** (run `28272040338`, 9 jobs success + Release skipped; verifiqué cada job —incl. el lento `Tests all extras`, `Regulatory coverage` y la matriz ubuntu/macos × 3.11/3.12— sin fiarme del agregado prematuro).

## Histórico — B5.6 (`ea0463e` · `eda/figures.py` + `eda/step.py` — ✓ HECHO, CI VERDE)
**Resultado:** el EDA ya corre como **paso del pipeline** (B5.6, 6/7 de A1 B5 EDA). `EdaStep(AuditableMixin)` (`@register("standard", domain="eda")`, `name="eda"`, `requires=(("data","frame"),("data","labels"))`, `provides` los 5 `("eda",k)`) integra los 5 analizadores: `execute(study, rng)` lee `data.frame`/`data.labels`, filtra por `cfg.analysis_partition` (lee `("data","splits")` solo si ≠ "todas"), aplica **muestreo opt-in** (si `cfg.sampling.enabled` y `len>max_rows`: `frame.sample(n=max_rows, random_state=rng)`), orquesta `default_rate→stability→univariate→quality→figuras`, publica los 5 artefactos con `study.artifacts.set("eda", …)` y retorna `EdaResult` (frozen). **NO muta ni escribe el dominio `data`** (invariante SDD §7). `FigureSpec` (`eda/figures.py`, frozen, `arbitrary_types_allowed`): receta declarativa `kind∈{line,bar,heatmap}`+`title` español+`data: DataFrame`+`x`/`y`/`series`; **NO importa matplotlib/plotly** (el render se difiere a report/SDD-26). `core/study.py` generaliza el cableado de dominios a mapeos declarativos `_DOMAIN_MODULES`/`_DOMAIN_CONFIG_CLASSES`/`_DEFAULT_DOMAIN_ORDER` (salda la deuda del `if name=="data"`; un dominio futuro es declarativo). `import nikodym.eda` ahora importa `eda.step` para correr el `@register` (igual que `data`): arrastra el stack tabular, pero **`import nikodym.core` sigue liviano** (verificado, gate verde). **El muestreo diferido de B5.4 quedó implementado aquí** (`log_decision(regla="muestreo_eda", umbral=max_rows, valor=n_original, accion="muestrear")`, exactamente un evento por muestreo).
- **🔴 DECISIONES AUTÓNOMAS (frontera, para revisión final de Cami):** (1) `splits` NO va en `requires` (solo `frame`+`labels`, fiel al backlog) → se lee condicionalmente cuando `analysis_partition≠"todas"`; (2) `FigureSpec` no importa libs de gráficos (contrato declarativo; render en report/SDD-26); (3) el auto-alcance de columnas de `univariate` excluye columnas datetime. Todas marcadas en docstrings/comentarios.
- **Gates locales (maestro, R7): verdes** — ruff + format(117) + mypy(68 source files) + **626 tests** (+16 vs B5.5) + cobertura **100% global** (3706 stmts, 0 miss) + regulatorio 100% + núcleo liviano + `uv build`.
- **Worker Codex:** reciclado fresco; construcción ~9.5 min, gates verdes a la 1ª (sin ciclos de re-trabajo).
- **Revisor adversarial (Claude Opus, motor cruzado vs Codex): APROBADO A LA 1ª.** Verificó (más allá de los gates): no-mutación del dominio `data` en ambas rutas (`"todas"` y `"desarrollo"`); muestreo emite **exactamente un** `decision/muestreo_eda` y ninguno sin muestrear (payload = SDD §9); reproducibilidad **bytewise con y sin muestreo** (verificó la ruta con muestreo a mano, más fuerte que los tests); núcleo liviano holds; `figures.py` sin matplotlib/plotly; golden NO tautológicos (`overall_rate==4/12` y `len(figures)==3` calculados independientemente); audit wiring correcto (`_run_one` inyecta `_audit`). **3 nitpicks NO materiales → deuda menor F1:** (1) `step.py` el comentario sobre exclusión de columnas datetime no aclara que es incondicional (benigno; solo auto-alcance `columns=None`); (2) falta un `assert_bitwise_reproducible` con `sampling.enabled=True` a nivel `EdaStep` (la garantía se cumple, verificada a mano; hoy cubierta solo indirectamente); (3) pasar `audit=self._audit` a `default_rate`/`univariate`/`quality` que hacen `del audit` (puros) puede confundir — documentar que es intencionalmente inerte.
- **Push `ea0463e`; CI en GitHub VERDE** (run `28270902476`, 9 jobs success + Release skipped; verifiqué cada job —incl. el lento `Tests all extras`, `Regulatory coverage` y la matriz ubuntu/macos × 3.11/3.12— sin fiarme del agregado prematuro).

## Histórico — B5.5 (`3eb639d` · `eda/quality.py` — ✓ HECHO, CI VERDE)
**Resultado:** existe el **perfilador de calidad descriptivo** (B5.5, 5/7 de A1 B5 EDA). `DataQualityProfiler` (no-estimador, `from_config`, sin fit/predict) produce, vía `profile(frame, *, audit=None)` (diagnostica TODAS las columnas del frame; sin `target_col`/`columns`), un `QualityResult` frozen con `by_column` (1 fila por columna) y columnas `col|dtype|missing_rate|cardinality|near_constant|near_unique|high_cardinality`. Copia defensiva (no muta el frame); valida frame no vacío + índice único (reusa `_validate_non_empty_frame`/`_validate_unique_index` de `default_rate`) fail-fast con `EdaError`. `missing_rate`=(#NaN tras neutralizar `±inf`→NaN)/#filas; `cardinality`=distintos finitos no-NaN (`nunique(dropna=True)`); `near_constant`=un valor concentra ≥`near_constant_threshold` de los no-nulos; `high_cardinality`=categórica (object/category/string) con cardinalidad >`high_cardinality_threshold` (numéricas→False). `±inf` se neutraliza **solo en numéricas** (`series.replace([np.inf,-np.inf],np.nan)`, lección B5.4). Los dtypes del `by_column` se fijan con `astype` (golden bit-idéntico estable). `eda/__init__.py` reexporta perezosamente B5.5 sin romper el núcleo liviano.
- **🔴 DECISIÓN AUTÓNOMA (frontera, para revisión final de Cami):** `QualityConfig` NO tiene umbral propio para `near_unique`. Se definió `near_unique = (n_no_nulos>0) and (cardinality/n_no_nulos ≥ near_constant_threshold)` (≈ "casi todas las filas no nulas son distintas", típico de identificadores), documentada en el docstring del módulo. **No se añadió config nueva** a propósito: un campo propio (p.ej. `near_unique_threshold`) cambiaría el golden `config_hash` de B5.1 (cerrado) y sería cambio de alcance. Si Cami quiere afinar el criterio, ese es el cambio.
- **Gates locales (maestro, R7): verdes** — ruff + format(113) + mypy(66) + **610 tests** (+10 vs B5.4) + cobertura **100% global** (3564 stmts, 0 miss) + regulatorio 100% + núcleo liviano + `uv build`.
- **Worker Codex:** reciclado fresco (panel previo quedó en tree-mode tras B5.4 → el reciclaje lo resolvió); construcción ~4.5 min, sin ciclos de re-trabajo (gates verdes a la 1ª).
- **Revisor adversarial (Claude, motor cruzado vs Codex): APROBADO A LA 1ª.** Verificó los golden con pandas crudo (no tautológicos), que `±inf` se trata como missing sin inflar `cardinality` ni romper bajo `filterwarnings=error` (lección B5.4 NO se repite), determinismo (20 corridas bit-idénticas), no-mutación incluso por las rutas categórica/inf, y fidelidad al SDD-27 §7 (incluida la desviación pre-aceptada: el snippet §4 omite `high_cardinality` pero §7 + el backlog la exigen → incluida). **3 nitpicks NO materiales → deuda menor F1:** (1) `near_unique` puede dar `True` sobre una columna CONSTANTE con n minúsculo (`cardinality≤1`) — inocuo en datos reales (N grande → ratio≈0), solo-reporta; afinar con `cardinality>1 and …`; (2) faltan tests de `near_unique` sobre constante-con-1-no-nulo y de dtypes `string`/`Int64` nullable (verificados a mano, funcionan); (3) `profile` no rechaza nombres de columna duplicados (a diferencia de `univariate.py`), solo alcanzable en uso standalone patológico.
- **Push `3eb639d`; CI en GitHub VERDE** (run `28269341802`, 9 jobs success + Release skipped; esperé `completed/success` y verifiqué el job lento `Tests all extras`, sin fiarme del agregado prematuro).

## Histórico — B5.4 (`7a10a0a` · `eda/univariate.py` — ✓ HECHO, CI VERDE)
**Resultado:** existe el **perfilador univariado descriptivo** (B5.4, 4/7 de A1 B5 EDA). `UnivariateProfiler` (no-estimador, `from_config`, sin fit/predict) produce, vía `profile(frame, *, target_col, columns, audit=None)`, un perfil feature↔target sobre la población elegible (`target∈{0,1}`): por columna una tabla `tramo|n|coverage|default_rate` donde `n`=elegibles del tramo, `coverage`=n/Σn (suma 1.0), `default_rate`=n_bad/n (NaN si n==0). Numéricas→`pd.qcut(n_quantile_bins, duplicates="drop")` (códigos en orden ascendente; etiqueta `str(Interval)`; numérica constante que `qcut` colapsa→1 tramo `[v,v]`; tramo "missing" explícito al final). Categóricas→un tramo por nivel agrupado con la clave estable `_stable_label` (orden lexicográfico determinista), niveles con freq<`rare_level_threshold`→`"_otros_"`, NaN→"missing". `descriptive_iv` (dict) **solo si `compute_descriptive_iv`** (Laplace α=0.5, K=tramos, IV=Σ(dist_good−dist_bad)·ln(dist_good/dist_bad), etiquetado pre-binning, default off→`{}`). Reusa el patrón de `default_rate.py` (importa `_eligible_mask`/`_bad_mask`/`_stable_label`/`_default_rate_series`/`_validate_*`). Copia defensiva (no muta el frame). `eda/__init__.py` reexporta perezosamente B5.4 sin romper el núcleo liviano.
- **🔴 DECISIÓN AUTÓNOMA (frontera, para revisión final de Cami):** `profile` es **función PURA sin muestreo ni `rng`**. El muestreo opt-in y su `log_decision(regla="muestreo_eda")` se **difieren a B5.6 (`EdaStep`)**, donde el `rng` inyectado y `EdaConfig.sampling` son accesibles: la firma del SDD-27 §4 de `profile` no recibe `rng` y `SamplingConfig` vive en `EdaConfig`, no en `UnivariateConfig`. El test de reproducibilidad ejerce el muestreo **aguas arriba** con `SeedManager(42).generator_for("eda")` (la muestra la toma el test, no `profile`).
- **Gates locales (maestro, R7): verdes** — ruff + format + mypy(65) + **600 tests** (+18 vs B5.3) + cobertura **100% global** (3511 stmts, 0 miss) + regulatorio 100% + núcleo liviano + `uv build`.
- **Worker Codex:** reciclado fresco; construcción inicial ~9 min + 1 ciclo de re-trabajo (~2 min) por el fix del revisor.
- **Revisor adversarial (Claude, motor cruzado vs Codex): APROBADO tras 1 RECHAZO, un hallazgo MATERIAL invisible a gates+100%:** con `±inf` en una numérica, `np.nanpercentile` generaba un borde NaN y `qcut(labels=False)` asignaba código NaN no solo a los ±inf sino a filas **finitas** fuera de los bordes → desaparecían silenciosamente de la tabla (verificado: 6 elegibles→Σn=2, coverage 0.333≠1.0, **viola los invariantes del SDD §6** "Σn==elegibles" y "coverage suma 1.0"; subconteo regulatorio silencioso). Fix: `series.replace([np.inf,-np.inf], np.nan)` antes de `missing` en `_numeric_profile` (espejo de `eda/stability.py:155`) → ±inf caen en el tramo "missing", Σn y coverage preservados; +test dedicado de inf (la estrategia Hypothesis fijaba `allow_infinity=False`, por eso los gates no lo veían). El revisor verificó el golden IV `1.0426…` a mano (no tautológico) y, en el ciclo 2, un fuzz de 250 trials con inf (0 invariantes rotos) + Float64 nullable + all-inf bajo `-W error`. **Nitpicks NO materiales → deuda menor F1:** doble copia (frame completo `univariate.py:108` + cada serie por columna `:121`); el `replace` de inf corre siempre aunque no haya infinitos (simetría con `stability.py`, dejar como está).
- **Push `7a10a0a`; CI en GitHub VERDE** (run `28268018692`, 9 jobs success + Release skipped; verifiqué cada job individual —incluido el lento `Tests all extras`— sin fiarme del agregado prematuro).

## Histórico — B5.3 (`adeca5a` · `eda/stability.py` — ✓ HECHO, CI VERDE)
**Resultado:** existe el analizador de **estabilidad temporal descriptiva** (B5.3, 3/7 de A1 B5 EDA). `TemporalStabilityAnalyzer` (no-estimador, `from_config`, sin fit/predict) toma el `DefaultRateResult` de B5.2 y, vía `assess(default_rate, *, audit=None)`, calcula sobre las tasas por período EXCLUYENDO `low_confidence`: `cv=σ/μ` (poblacional, `μ==0`→NaN sin warning), `max_relative_drift=max|rate−μ|/μ`, `trend_slope` (OLS numpy puro). `flagged = indicador[cfg.metric] > cfg.threshold`; si `flagged` emite **exactamente un** `log_decision(regla="estabilidad_temporal", umbral, valor, accion="senalar_redesarrollo")` (AuditableMixin) — **SEÑALA, NO aborta**. `<2 períodos` evaluables → `cv/drift/slope=NaN`, `flagged=False`, `accion="no_evaluable"`. `StabilityResult` BaseModel frozen. Copia defensiva del frame. `eda/__init__.py` ahora reexporta perezosamente (vía `__getattr__`) B5.2 **y** B5.3 sin romper el núcleo liviano.
- **Gates locales (maestro, R7): verdes** — ruff + format(109) + mypy(64) + **582 tests** (+16 vs B5.2) + cobertura **100% global** (3344 stmts, 0 miss) + regulatorio 100% + núcleo liviano + `uv build`.
- **Worker Codex:** reciclado fresco; construcción inicial ~5.5 min + 3 ciclos de re-trabajo (~1.5 min c/u).
- **Revisor adversarial (Claude, motor cruzado vs Codex): APROBADO tras 3 RECHAZOS, todos MATERIALES de la misma clase (orden de filas distorsiona la señal regulatoria) invisibles a gates+100%:** (1) `trend_slope` sobre orden POSICIONAL → ordena por `period` (`mergesort`); (2) `na_position="last"` no excluía el bucket de período NaT que B5.2 produce con `groupby(dropna=False)` → `dropna(subset=["period"])`; (3) `assess` ignoraba `axis` → cohortes ordenadas lexicográficamente invertían el signo de `trend_slope` o lanzaban `TypeError` → **decisión del maestro: restringir al eje temporal `axis="period"` con `EdaError`** (`_validate_temporal_axis`). El maestro hizo 3 ciclos (excedió el "máx 2" porque cada uno CONVERGÍA con fix trivial y de bajo riesgo, no era un bucle sobre el mismo error; tope duro de revertir si el 3º no aprobaba — aprobó). **Nitpicks NO materiales → deuda menor F1:** (a) `fillna(True).astype("bool")` en `_evaluable_rates` puede emitir `FutureWarning` si un caller standalone pasa `low_confidence` con dtype object (B5.2 siempre emite bool real → no rompe hoy; con `filterwarnings=error` rompería un caller raro) → usar `.infer_objects(copy=False).astype("bool")`; (b) la rama `.fillna(True)` (confianza desconocida→excluir) no tiene test dedicado.
- **Push `adeca5a`; CI en GitHub VERDE** (run `28265966509`, esperé `completed success` sin fiarme del agregado prematuro).

## Histórico — B5.2 (`b163979` · `eda/default_rate.py` — ✓ HECHO, CI VERDE)
**Resultado:** existe el 1er analizador del EDA. `DefaultRateAnalyzer` (no-estimador, `from_config`, sin fit/predict) calcula la tasa de default por **período** (discretiza la fecha con `to_period(period_freq)`; infiere la única columna datetime o exige `date_col`; `EdaError` si ambiguo/ausente/no-datetime; quita tz sin warning) o **cohorte**, sobre la población elegible (`target∈{0,1}`; `<NA>` y valores fuera de contrato cuentan en `n_total` pero NO en el denominador). `DefaultRateResult` (BaseModel frozen) con tabla `by_period` (`period|n_total|n_eligible|n_bad|default_rate|low_confidence`) y `overall_rate` **ponderado por elegibles** (`Σn_bad/Σn_eligible`, NO promedio de tasas). `n_eligible==0`→`NaN` sin `ZeroDivisionError`. Copia defensiva del frame (no muta el input). `audit` aceptado pero no usado (analizador puro, documentado).
- **Gates locales (maestro, R7): verdes** — ruff + format(107) + mypy(63) + **566 tests** (+25 vs B5.1) + cobertura **100% global** + regulatorio 100% + núcleo liviano + `uv build`.
- **Worker Codex:** construcción inicial ~7 min; reciclado fresco al arrancar (1 tarea).
- **Revisor adversarial (Claude, motor cruzado vs Codex): APROBADO tras 2 ciclos de RE-TRABAJO.** Halló 2 bugs **MATERIALES** que los 4 gates + 100% cobertura NO vieron: **(1)** índice duplicado → `pd.DataFrame({...}, index=source.index)` rompía la alineación y devolvía `n_eligible/n_bad=0`, `default_rate=NaN` para todo, SIN error (número regulatorio errado y silencioso) → fix `_validate_unique_index` fail-fast con `EdaError` (espejo de `data/hashing.py`/`partition.py`). **(2)** representante de grupo inestable en cohortes de tipos mixtos: `groupby` por valor crudo colapsaba `int 2024`/`float 2024.0` (y `1`/`True`) en un grupo cuyo representante dependía del orden de filas → no bitwise-reproducible (viola SDD-27 §6/§9) → fix `_resolve_group_frame` agrupa por clave estable tipo-consciente (`_stable_label` = `tipo:repr`) y deriva la etiqueta visible de forma determinista; orden por la clave estable. + tests que ejercitan ambos bugs (fallarían sin el fix). **Nitpicks NO materiales → deuda menor F1:** la etiqueta visible de una cohorte object mixta int/float se upcastea a float (cosmético, determinista, no altera números; solo en columnas mixtas patológicas); el golden `[2024.0, 2024]` pasa pero el 2º representante es float (comentario sobrevende el dtype).
- **Push `b163979`; CI en GitHub VERDE** (run `28263453699`, 9 jobs success + Release skipped; esperé el job lento `Tests all extras`).

## Histórico — B5.1 (`3e4b7e5` · `eda/config.py` + cableado `eda` — ✓ HECHO, CI VERDE)
**Resultado:** la sección `eda` ya existe en `NikodymConfig` y B5.2–B5.7 pueden engancharse. El worker Codex creó el paquete `eda/`: `config.py` (`EdaConfig` + `DefaultRateConfig`/`TemporalStabilityConfig`/`UnivariateConfig`/`QualityConfig`/`SamplingConfig`, todas `NikodymBaseConfig` frozen/forbid, cada campo con `title`/`description` español/rangos `ge-le`/`Literal`/`ui_*`, espejo de `data/config.py`), `exceptions.py` (`EdaError(NikodymError)`), `__init__.py` (registra el hook `_EDA_CONFIG_CLS` con import perezoso → núcleo liviano intacto). Cableado en `core/config/schema.py`: hook `_EDA_CONFIG_CLS` + `field_validator("eda", mode="before")` (`_valida_eda`, espejo EXACTO de `_valida_data`) + campo `eda`. **`eda` NO es INFRA → ENTRA al `config_hash`** (golden recomputado `08d94c4c…` con `eda: null` en el payload canónico). `strategies.py` añadió `sections=["eda"]` y separó `_REGISTRY_SECTION_MODULES` (sin `eda`: su `Step @register` llega en B5.6).
- **Gates locales (maestro, R7): verdes** — ruff + format(105) + mypy(62) + **541 tests** (+15) + cobertura **100% global** + regulatorio 100% + núcleo liviano + `uv build`.
- **Worker Codex:** auto-update `0.142.0→0.142.2` al 1er arranque tiró el 1er goal a `zsh` (la trampa documentada) → re-reciclado limpio; 2º arranque OK. ~8.5 min de construcción.
- **Revisor adversarial (Claude, motor cruzado vs Codex): APROBADO** — verificó cada default/rango vs SDD-27 §5, `_valida_eda` espejo fiel del blob opaco, golden de defaults NO tautológico, `eda` ∉ INFRA, núcleo liviano holds. 4 nitpicks cosméticos (descripción "(solo reporte)" en `near_constant_threshold`; comentario de por qué numpy no se bloquea en el test liviano; docstring de `discriminated_union_tags` explicando por qué `eda` queda fuera del registry hasta B5.6; `max_rows` con `description` que el SDD literal omitía = mejora) → **deuda menor F1**.
- **Push `3e4b7e5`; CI en GitHub VERDE** (run `28260621149`, 9 jobs success + Release skipped; esperé el job lento `Tests all extras`).

## Histórico — FIX-target (ítem 11 · `f552c72` — ✓ HECHO, CI VERDE)
**Resultado:** el etiquetado del target ya no produce dos cifras regulatorias erradas. (a) Una fila con `observation_date` o `data_cutoff` en `NaT` ya NO se etiqueta como bueno/malo por defecto (censura insegura): `_window_incomplete_mask` la marca como ventana incompleta → `excluido(ventana_incompleta)`. (b) `_apply_exclusions` ya NO subcuenta: `exclusions_by_reason` **acumula** (`get(reason,0)+count`) y un `model_validator` en `TargetConfig` rechaza nombres de exclusión duplicados y la colisión con la razón reservada `EXCLUSION_WINDOW_REASON="ventana_incompleta"` (movida a `data/config.py`, la capa base, para evitar import circular). +6 tests TDD.
- **Gates locales (maestro, R7): verdes** — ruff + format + mypy(59) + **526 tests** + cobertura **100% global (3058 stmts, 0 miss)** + regulatorio 100% + núcleo liviano + `uv build`.
- **Revisor adversarial (Claude, motor cruzado vs worker Codex): APROBADO a la 1ª.** Reprodujo a mano el golden de acumulación (`=2`, no tautológico), verificó los 3 casos de NaT (solo obs / solo cutoff / ambos) → máscara booleana pura sin NaN, confirmó que mover la constante no introduce import circular ni rompe el núcleo liviano, y que la copia defensiva del df se preserva. 3 nitpicks **cosméticos** (comentarios de auditabilidad) → deuda menor F1 (ver ítem 11).
- **Push `f552c72`; CI en GitHub VERDE** (run `28189548182`, 9 jobs success; esperé a que terminara el job lento `Tests all extras` —instala `--all-extras`— antes de cerrar, no me fié del agregado prematuro de `gh run list`).
- **Contexto:** 2ª corrida autónoma productiva del día (tras FIX-hashing 13:25). **Cierra el "🔧 Backlog de FIXES" → cola autónoma AGOTADA salvo R0.** Próxima corrida → paso 3.5 (replanificar T2/F1) o corrida vacía (ver Próximos pasos).

## Histórico — FIX-hashing (B2b.3 · `019ca1b` — ✓ HECHO, CI VERDE)
`data_hash` ya no devuelve un hash no determinista ante índice duplicado: `_validate_unique_index` lo rechaza con `DataValidationError` antes de hashear (fail-fast, consistente con FIX-partition). Algoritmo y golden `v1` intactos para índice único. +2 tests (índice duplicado→raises; `category` golden cross-OS). 520 tests 100%; revisor adversarial APROBÓ a la 1ª; CI verde (run de `019ca1b`). 2 nitpicks deuda menor F1 (ítem 10).

## Histórico — F0 CERRADO (B4.3 v2 · CI — ✓ HECHO, CI VERDE)
**Resultado: F0 cerrado.** El worker Codex recuperó el CI del 1er intento (`git cherry-pick -n 11ea69a`) y aplicó los 3 fixes del plan v2; el maestro revisó (R7), validó gates locales, pusheó (`6e55be8`) y **confirmó el 1er run del CI en GitHub VERDE** (`28156768899`) antes de cerrar.
- **3 fixes aplicados sobre el v1:**
  1. **anti-copyleft robusto** (job `build`, paso "Verify runtime licenses"): regex que falla SOLO ante copyleft real (`AGPL|LGPL|GPL` + variantes "GNU … General Public License"); licencias propietarias no-copyleft (`LicenseRef-*`/`nvidia-*`) y metadata vacía/no clasificable → status `metadata_incomplete`/`ok` = **warning, NO falla**. Resuelve el falso positivo `nvidia-nccl-cu12` (solo aparece en Linux). Evalúa markers de plataforma; sube `license-report.json` como artifact.
  2. **hash-EOL**: `tests/unit/test_audit_environment_hashing.py` usa `write_bytes` en vez de `write_text` (bytes idénticos en todo OS; golden intacto, 516/516).
  3. **matriz acotada**: job `test` solo `ubuntu+macos × 3.11/3.12`; comentario en el yaml documenta la desviación de SDD-25 §7 (Windows→B4.4, 3.13→B4.5).
  + perfiles Hypothesis `dev`/`ci`/`nikodym_deterministic` centralizados en `conftest.py` (+`test_data_partition.py` ya no pisa el perfil del CI); bump `numba>=0.60`/`llvmlite>=0.43` del extra `explain`.
- **Gates locales (maestro, R7): TODOS verdes** — ruff + format(101) + mypy(59) + **516 tests** + cobertura **100%** + regulatorio 100% + núcleo liviano + `uv build` + `uv lock --check` + YAML válido.
- **CI real en GitHub (run `28156768899`): VERDE.** 9 jobs success (Quality, Build, Regulatory coverage, Tests ×4 [ubuntu/macos × 3.11/3.12], Tests all extras, Lock check); Release skipped (solo tag `v*`). Annotation benigna: Node.js 20 deprecado en `actions/checkout@v4`+`setup-uv@v5` (forzado a Node 24; deuda menor → subir a `@v5`/última cuando convenga, no urge).

## En curso / a medias
Nada a medias. main avanzó con **B5.5** (`3eb639d`, CI verde): `eda/quality.py` [`DataQualityProfiler` + `QualityResult`], diagnóstico descriptivo de calidad por columna (missing/cardinalidad/near_constant/near_unique/high_cardinality; solo reporta). Árbol limpio, lock IDLE. **A1 B5 EDA en curso (5/7): B5.1 ✅ + B5.2 ✅ + B5.3 ✅ + B5.4 ✅ + B5.5 ✅** → **la próxima corrida ejecuta B5.6** (`eda/figures.py`: `FigureSpec` declarativa kind∈{line,bar,heatmap}, NO renderiza/NO importa matplotlib·plotly; + `eda/step.py`: `@register("standard", domain="eda")` `EdaStep(AuditableMixin)` + `EdaResult`, orquesta default_rate→stability→univariate→quality→figuras y publica los 5 artefactos `("eda", k, v)` sin mutar `artifacts["data"]`; + cableado `Study.run(steps=[...,"eda"])`: extender `_ensure_domain_registered`/`_coerce_domain_config` para `"eda"` (import perezoso, **generaliza el mapeo dominio→módulo, salda deuda**) y `_default_step_names` añade `"eda"` tras `"data"`; ⚠ es donde se difirió el **muestreo + `log_decision(regla="muestreo_eda")`** de B5.4 [`rng` inyectado + `EdaConfig.sampling`]; SDD-27 §2/§4/§6/§7/§10 + CT-1; ver backlog B5.6). Luego B5.7, después A2 B4.5 → A3 B4.4 → A4 deudas F1 → A5 diseña-y-aplica (binning..report). F0 + FIXES cerrados, CI verde. Único hard-stop: R0-C release público.

## Próximos pasos
**Inmediato (autónomo, ventana 16h 2026-06-26): EJECUTAR B5.6** (`eda/figures.py` + `eda/step.py` + cableado `Study.run`). Es el ítem **integrador** de EDA: (1) `eda/figures.py` `FigureSpec` declarativa (`kind`∈{line,bar,heatmap}, `title` español, `data`, `x`, `y`, `series`) = receta+datos, **NO renderiza, NO importa matplotlib/plotly**; (2) `eda/step.py` `EdaStep(AuditableMixin)` `@register("standard", domain="eda")` + `EdaResult` — `Step` Protocol (`name="eda"`, `requires=(("data","frame"),("data","labels"))`, `provides=` los 5 artefactos `"eda"`); `execute(study, rng)` lee `artifacts["data"]`, filtra por `cfg.analysis_partition`, orquesta default_rate→stability→univariate→quality→figuras y publica `study.artifacts.set("eda", k, v)` **sin mutar `artifacts["data"]` ni el frame**; (3) en `core/study.py` extender `_ensure_domain_registered`/`_coerce_domain_config` (hoy `if name=="data"`) para `"eda"` con **import perezoso** (generaliza el mapeo dominio→módulo = **salda la deuda HANDOFF**) y `_default_step_names` añade `"eda"` tras `"data"` si la sección está activa. **🔑 AQUÍ se materializa el muestreo diferido de B5.4** (`SamplingConfig`/`EdaConfig.sampling` + `rng` inyectado): si `cfg.sampling` activa, muestrear ANTES de los analizadores con `rng` (semilla del `SeedManager`) y emitir `log_decision(regla="muestreo_eda")` vía `AuditableMixin` (una sola vez). SDD-27 §2/§4/§6/§7/§10 + CT-1. tests: `tests/unit/test_eda_step.py` + `tests/integration/test_eda_pipeline.py` (`study.run(steps=["data","eda"])` puebla los 5 artefactos; frame idéntico antes/después; `assert_bitwise_reproducible`). **⚠ B5.6 TOCA `core/`** → re-verificar el gate de núcleo liviano tras el cableado. **Lecciones a meter en el goal:** (a) el revisor adversarial es implacable con reproducibilidad bit-idéntica y orden — cuidar el muestreo determinista por `rng` y el orden de artefactos; (b) leer ANTES `core/study.py` (`_resolve_step`/`_ensure_domain_registered`/`_coerce_domain_config`/`_default_step_names`, patrón `data` de B2d) y `data/step.py`/`data/card.py` como espejo; (c) el worker NO commitea. Worker Codex → gates + revisor + `gh run` CI verde, cierra. Luego **B5.7** (`eda/card.py`), después A2 B4.5 → A3 B4.4 → A4 deudas F1 → A5 diseña-y-aplica (binning..report). **Hay combustible de sobra para 16h.** Revisor obligatorio + CI verde antes de cerrar cada ítem. **NO** ejecutar R0-C (release público). Si todo se agota o una fase se bloquea sin diseño seguro, auto-pausa limpia (sin churn, lock IDLE).
**F0 cerrado.** Siguiente fase real: **T2 scoring → F1 → release público v0.1.0**, todo gateado por R0 (ver bloque R0 arriba): producir SDD-06..11/26, aprobar SDD-27→EDA (B5.1–B5.7 ya troceado por el planificador), y la prioridad de fase (D-prioridad de Cami). Para reanudar avance autónomo, Cami debe trocear/aprobar una fase o aprobar B4.4/B4.5.
**Método validado (mantener):** worker Codex un-trozo/commit → revisión + gates locales + push → **esperar `gh run view` del CI verde antes de cerrar** (lección B4.3: gates locales verdes ≠ CI verde; ahora el CI lo confirma automáticamente en cada push).
- **B4.4 / B4.5 (DECISIÓN CAMI = R0):** soporte **Windows** (`Study.save` Windows-safe) y **Python 3.13** (ortools≥9.11 o excluir). El CI va con matriz acotada hasta que Cami los priorice; al resolverlos, ampliar la matriz del CI (añadir `windows-latest` / `3.13`).
- **Deuda CI menor (no bloquea):** actualizar `actions/checkout`/`astral-sh/setup-uv` a versiones sobre Node 24 cuando salgan (hoy Node 20 deprecado → forzado a 24, benigno).
- **Deuda B4.1 → F1 (3 menores, NO bloquean):** (1) quitar el test redundante/tautológico `test_golden_seed_sequence_equivale_a_seed_manager` (el golden hardcodeado ya protege); (2) `dummy_step_config` deja el Step dummy en el `REGISTRY` global sin limpiar → fixture con cleanup / `REGISTRY.unregister` para suites largas; (3) documentar el caveat de orden de `pickle` en `reproducibility._canonical_bytes`.
- **Deuda abierta de B3.1c (diferida):** decidir si `MLflowInventory.register` debe seguir duplicando las métricas como tags `nikodym.metric.*` o alinearse con SDD-04 §4 (métricas solo como *metrics del run*). + endurecer la doble escritura de tags y unificar `_registry_db_backed` (DRY) en `tracking/inventory.py`+`recorder.py`. Benigno; resolver al consumir el inventario o en limpieza de F1.
- Pendiente de B3.1b (si se vuelve a tocar el model card): congelar `ScenarioLog.path`. _(El `deepcopy`/snapshot de `metric_sections` ya se resolvió en B4.2.)_
- Cuando F0+ sume más dominios orquestables, **generalizar** el mapeo dominio→módulo de `Study._ensure_domain_registered`/`_coerce_domain_config` (hoy `if name == "data"`, ver deuda abajo).

## Decisiones / contexto a recordar
- **B4.3 v2 CI (CERRADO, CI verde) — decisiones/lecciones:** (a) **el checker anti-copyleft debe fallar SOLO ante copyleft real** (regex `AGPL|LGPL|GPL` + "GNU … General Public License"), nunca ante licencias propietarias no-copyleft (`LicenseRef-NVIDIA-Proprietary`) ni metadata ausente/no clasificable (→ warning, no falla). Diseño **robusto por construcción**, no por allowlist por-paquete. El falso positivo `nvidia-nccl-cu12` del v1 quedó resuelto y el job `build` pasó en Linux. (b) **lección de la corrida previa CUMPLIDA**: tras push, esperé el 1er run del CI en GitHub (`gh run view` → `28156768899` success) ANTES de marcar `[x]`. Gates locales verdes ≠ CI verde, pero esta vez coincidieron. (c) **`git cherry-pick -n <commit-revertido>`** es la vía limpia para recuperar trabajo de un commit revertido al working tree sin commitear (el worker partió del v1 `11ea69a` así). (d) la **matriz acotada** (ubuntu+macos × 3.11/3.12) es desviación deliberada de SDD-25 §7, documentada en el propio yaml; Windows/3.13 son R0 (B4.4/B4.5).
- **B4.3 CI (1er intento revertido `11ea69a`→`68db957`) — histórico/lecciones:** (a) **gates locales verdes ≠ CI verde**: ruff/mypy/pytest/cobertura/build/regulatorio/liviano pasaron al 100% en local (Python 3.12, macOS) y aun así el CI nació rojo en Windows, 3.13 y anti-copyleft (Linux). (b) **el checker anti-copyleft debe fallar solo ante copyleft real** (resuelto en v2). (c) **`Study.save` no es Windows-safe** (swap `os.replace` sobre destino existente → WinError 5) y había **hashes golden sensibles a EOL** (write_text→CRLF; el EOL se mitigó en v2, `Study.save` sigue pendiente B4.4). (d) **`ortools` (optbinning/scoring) no soporta 3.13** en la versión lockeada (B4.5). (e) el **bump `numba`/`llvmlite`** del extra `explain` fue correcto (versiones viejas sin wheels Py3.12). (f) revertir > parchear a ciegas cuando los fallos no son reproducibles localmente y tocan alcance (R0).
- **B4.2 criterios Hito 0 — decisiones `_CONTRATOS-TRANSVERSALES.md §4`:** (a) los 3 criterios viven juntos en `tests/unit/test_hito0_contracts.py` (pruebas de fuego de contratos, no de un módulo) + un `src/` mínimo (`testing/regulatory.py`) para la fuente única reutilizable por B4.3. (b) **CT-1 fan-in** se prueba con steps dummy que implementan la interfaz `Step` por duck-typing e inyectados vía `monkeypatch` de `Study._resolve_steps` (no toca el REGISTRY global → sin drift entre tests); el caso negativo confirma que la **validación pre-run** del motor (`Study.run`) lanza `ConfigError` con "inejecutable" ANTES de ejecutar paso alguno (status sigue `created`). (c) **CT-2** se endureció con `deepcopy` (snapshot profundo) en `ModelCard.metric_sections` y `OverlayRecord.payload` vía `field_validator(mode="before")` — la firma SÍ aguantaba payload estructurado (dict anidado / listas), solo faltaba la copia defensiva; **de paso cierra la deuda B3.1b** (antes copia superficial). Golden bit-idéntico con `now`/`environment_provider` inyectables. (d) **gate regulatorio**: la verdad de "qué módulos van a 100%" pasa a vivir en código (`REGULATORY_COVERAGE_PATHS`) en vez de solo en el comando del gate; `REGULATORY_COVERAGE_INCLUDE` se deriva quitando el prefijo `src/` y queda coherente con el `--include` de `docs/AUTONOMY.md`. El test falla ruidoso si un path declarado no existe (cierra el falso verde por vacuidad `0/0=100%`). (e) `provisioning/cmf/__init__.py` e `ifrs9/__init__.py` siguen vacíos (0 stmts) pero DEBEN existir; el gate ahora lo verifica.
- **B4.1 `testing` — decisiones SDD-24:** (a) **paquete `src/nikodym/testing/`** distribuible (va en el wheel) pero **NO contamina el núcleo liviano**: `import nikodym.core` no arrastra `hypothesis`; dependencia unidireccional `testing → core` (core nunca importa testing). (b) **Hypothesis NO se expone como extra público `[testing]`**: queda como `dependency-group` dev/test (PEP 735) + **import perezoso dentro de funciones** en `strategies.py` con mensaje claro si falta. (c) gate de núcleo liviano (`docs/AUTONOMY.md`) ampliado con `hypothesis`. (d) los **9 checks** de `check_nikodym_estimator` mapean 1:1 a SDD-24 §7.2; cada uno con un test negativo. (e) **`golden_seed_sequence` con golden test fuerte** (valores hardcodeados de `SeedManager(42)`). (f) `dummy_step_config` registra un Step dummy **idempotente** en el `REGISTRY` global. (g) `assert_bitwise_reproducible` compara vía `pickle` proto 5 con `normalize` opcional. (h) **🟡 deuda menor → F1** (ver Próximos pasos).
- **B3.1c `tracking` — decisiones SDD-04 + CT-4:** (a) **paquete `tracking/` fuera de `core`** + `src/nikodym/api.py` como capa fina de orquestación (CT-4: `assemble_run` no vive en `core`). (b) **MLflow import perezoso** vía `utils/optional.require_extra("tracking","mlflow")` SIEMPRE dentro de métodos; `mlflow` en extra `[tracking]` (≥2.10), NO en deps; gate liviano ampliado con `mlflow`/`nikodym.tracking`. (c) **cableado idéntico a `audit`/`governance`**: hook `_TRACKING_CONFIG_CLS` + `field_validator` + campo `Any`-runtime/`TrackingConfig`-`TYPE_CHECKING`; `tracking` es sección **INFRA**. (d) **`MLflowInventory` implementa el Protocol `ModelInventory` de B3.1b**; idempotencia por `(model_name, config_hash)` (D-TRK-7). (e) **`assemble_run` resuelve inventario por `governance.publish_to_inventory`**: True+extra→`MLflowInventory`; True+sin extra→`MissingDependencyError`; si no→`NullInventory`. Compone `FanOutSink([JsonlAuditSink, TrackingSink])`. (f) **`autolog=False` (D-TRK-3), aliases no stages (D-TRK-8), `fail_on_tracking_error=False` degrada a warning+no-op**. (g) **reloj/cliente/módulo mlflow inyectables** → golden bit-idéntico, tests con fakes deterministas sin requerir mlflow. (h) **🟡 deuda NO bloqueante:** `register` duplica `entry.metrics` como tags `nikodym.metric.*` vs nota SDD-04 §4 — benigno; decidir en limpieza F1. + DRY de `_registry_db_backed`.
- **B3.1b `governance` — decisiones SDD-03 §4.2:** (a) **cableado idéntico a `audit`/`data`** (hook `_GOVERNANCE_CONFIG_CLS` + `field_validator` + campo `Any`-runtime/`GovernanceConfig`-`TYPE_CHECKING`). (b) **`governance` es sección INFRA** → excluida de `config_hash`. (c) **`ModelCardBuilder` con `now`/`environment_provider` inyectables** → model card golden bit-idéntico. (d) **`_add_months` stdlib** (`calendar.monthrange`) en vez de `pandas.DateOffset` → `next_review_date` sin arrastrar pandas; el `data_card` se referencia como `Any`/`TYPE_CHECKING`. (e) **`DecisionRecord` con campos en español** (`regla`/`umbral`/`valor`/`accion`; tolera alias histórico `acción`). (f) **`ModelInventory` = solo Protocol `@runtime_checkable` + tipos** (impl `MLflowInventory` es B3.1c). (g) **`RegistryUnavailableError` en `governance/exceptions.py`**. (h) obs menores: ~~copia superficial de `metric_sections`~~ **resuelta en B4.2**; `ScenarioLog.path` mutable (sigue abierta, benigna).
- **B3.1a `audit` — decisiones SDD-03 §4.1/§5:** (a) **JSONL canónico append-only** (`sort_keys=True`+`separators=(",",":")`+`ensure_ascii=False`); el sink reabre en `"a"`, nunca reescribe. (b) **`capture_environment` con providers inyectables** (`now`/`version_provider`/`python_version_provider`/`platform_provider`) → snapshot determinista. (c) **`hash_dataframe` REUSA `data_hash`** (import perezoso); `algo` distinto de `sha256` falla ruidoso. (d) **replay tolerante a truncamiento** (cola corrupta → warning; corrupción+línea válida → `AuditError`). (e) **cableado `NikodymConfig.audit`** (hook diferido `_AUDIT_CONFIG_CLS`). (f) **`audit` es sección INFRA**. (g) `AuditConfig.capture_environment`/`tracked_packages` quedan inertes hasta `api.assemble_run` (B3.1c) — esperado.
- **B2d `DataStep` + cableado `Study.run` — decisiones SDD-02 §4/§7 + CT-1:** (a) `@register("standard", domain="data")` (discriminador `type="standard"`). (b) **núcleo liviano preservado**: `Study._resolve_step` fuerza `importlib.import_module("nikodym.data")` **perezoso dentro del método** (`_ensure_domain_registered`). (c) `_default_step_names` deriva el pipeline v1 de secciones activas en orden de declaración (CT-1; scheduler topológico → F5). (d) el resolver acepta `Step` nativo o adapta `BaseNikodymEstimator` vía `StepAdapter`. (e) **inyección de datos en memoria** por `("data","input_frame")` (`INPUT_FRAME_KEY`) → end-to-end sin I/O. (f) `data_card` como módulo propio `data/card.py`. (g) `data/__init__.py` cierra la reexportación diferida de B2b/B2c + DataStep/DataCardSection.
  - **🟡 Deuda menor (para B3+):** `Study._ensure_domain_registered`/`_coerce_domain_config` tienen el mapeo dominio→módulo hardcodeado como `if name == "data"`. Pragmático con un dominio; al sumar binning/scoring/etc. conviene generalizarlo (tabla dominio→paquete o entry-points). NO bloquea.
- **Desviaciones de SDD-02 a ratificar (B2a; integrador):**
  - **`model_rebuild()` del SDD §5 NO funciona** (Pydantic 2.13). Reemplazo: **hook `_DATA_CONFIG_CLS` + `field_validator`**; `nikodym.data` lo puebla al importarse.
  - **`Predicate.value` SIN `strict=True`** (revienta sobre una unión en Pydantic 2.13). Modo unión smart (default) + orden `bool|int|float`.
  - **Defaults de `Field` en keyword `default=`** (no posicional; mypy sin plugin pydantic).
  - **`pandera>=0.24` con `import pandera.pandas as pa`** (NO `import pandera`: `FutureWarning` desde 0.24). **`pyarrow>=14`**.
  - Acumuladas previas (B1): `pandas-stubs`/`joblib.*` overrides; `StepAdapter` `*, requires/provides`; `mypy` en 3.12 no 3.11.
- **Hook `_DATA_CONFIG_CLS` (cómo razonar sobre tests):** es global de proceso. Tests core-only (`test_config_schema`, `test_config_loader`) lo neutralizan con `monkeypatch.setattr(schema, "_DATA_CONFIG_CLS", None)`; `test_data_config` lo fija a `DataConfig`. Hook None → `data` blob JSON-canónico opaco; hook seteado → se coacciona a `DataConfig` (extra=forbid). B2d depende de este coacheo en `Study._coerce_domain_config`.

## Callejones sin salida / no reintentar
- **CI ≠ gates locales (B4.3, 2026-06-25):** los gates locales (1 OS, 1 Python, sin `--all-extras`) NO detectan fallos de **Windows**, **Python 3.13** ni del **closure completo de licencias (Linux)**. Un commit con gates locales 100% verdes puede romper el CI. → Tras pushear algo con CI, **verificar el run real (`gh run view`)** antes de cerrar el ítem. _(En B4.3 v2 se cumplió: CI verde confirmado.)_
- **El gate anti-copyleft NO debe fallar ante licencias no-copyleft no listadas (B4.3) — RESUELTO en v2:** el checker (`build`→"Verify runtime licenses") ahora bloquea SOLO copyleft real (regex GPL/LGPL/AGPL + "GNU … General Public License"); propietarias (`LicenseRef-*`/`nvidia-*`) y metadata ausente pasan con warning. NO volver a la allowlist permisiva por-paquete (frágil).
- **Hashes golden de archivos sensibles a EOL (B4.3) — MITIGADO:** crear archivos de prueba con `write_text` mete `\r\n` en Windows → sha256 distinto. Usar `write_bytes`/`newline=""` para contenido que se va a hashear (aplicado en `test_audit_environment_hashing.py`). Revisar este patrón en tests nuevos que hasheen archivos.
- **`Study.save` no es Windows-safe (B4.3):** el swap atómico (`core/study.py:372`, `os.replace` `estudio`→`.estudio.old.xxx`) lanza `PermissionError [WinError 5]` en Windows. Pendiente de B4.4 (si Cami aprueba soporte Windows).
- **`ortools` sin wheel cp313 (B4.3):** `ortools==9.10.4067` (vía optbinning/scoring) solo trae cp311/cp312 → bloquea Python 3.13 en cualquier OS. Pendiente de B4.5.
- **NO `from nikodym.data...` (ni `audit`/`governance`/`tracking`/`mlflow`) a nivel de módulo en `core/`** — rompe el núcleo liviano (gate `import nikodym.core` sin data/pandera/pyarrow/pandas/tracking/mlflow). El registro de dominios se puebla por **import perezoso DENTRO de método** (`importlib.import_module` en `Study._ensure_domain_registered`); las secciones `audit`/`governance`/`tracking` son `Any` en runtime + hook diferido poblado al importar el paquete. NUNCA el import a nivel módulo en `core/`.
- **NO comparar valor vs columna de dtype incompatible sin guardia** (B2b.4 + B2c.1) — `series.eq(x)`/`.lt(x)` cross-dtype emite `FutureWarning` → rompe con `filterwarnings=error`. Filtrar por dtype ANTES (`_is_comparable` en special, `_validate_value_compatible` en target) y `comparison.fillna(False).astype(bool)`.
- **NO usar `model_rebuild()` para narrar `NikodymConfig.data`** — no funciona en Pydantic 2.13. El hook+validador es la vía.
- **NO `Field(strict=True)` sobre uniones** en Pydantic 2.13 — usar el modo smart (default).
- **NO defaults posicionales en `Field(...)`** si el modelo es target de `default_factory` — keyword `default=`.
- **`ruff` respeta `.gitignore`**: si un paquete fuente cae bajo un patrón ignorado, ruff lo SALTA silenciosamente (verde falso). Verificar el wheel/`git status` cuando se añade un paquete nuevo. El patrón de datos es `/data/` (raíz), no `data/`.
- **NO `import pandera as pa`** (top-level) — `FutureWarning` rompe con `filterwarnings=error`. Usar `import pandera.pandas as pa`.
- **NO `.values.tobytes()` para hashes reproducibles** (B2b.3) — es endian-NATIVO. Forzar little-endian: `hash_pandas_object(...).to_numpy(dtype="<u8", copy=True).tobytes()`. Normalizar `-0.0→0.0` y pinear `hash_key`/`encoding`/`categorize`.
- Heredados de B1 (siguen vigentes): NO cobertura por submódulo (`--cov=nikodym.core.audit` → doble-load numpy); `ArtifactStore.keys()` → `# noqa: SIM118`; colisión módulo/función `migration` (usar `from ... import X`); workflow JS: `parallel()` ya devuelve array, prompts sin backticks internos, `Date.now()`/`Math.random()` prohibidos.

## Comandos verde
- Sync: `uv sync --no-default-groups --group test --group lint --python 3.12`
- Verificar: `uv run --no-sync ruff check . && uv run --no-sync ruff format --check . && uv run --no-sync mypy && uv run --no-sync pytest -q --cov=nikodym --cov-report=term-missing`
- Gate regulatorio (100%): `uv run --no-sync coverage run -m pytest && uv run --no-sync coverage report --include="*/nikodym/core/exceptions.py,*/nikodym/core/seeding.py,*/nikodym/provisioning/cmf/__init__.py,*/nikodym/provisioning/ifrs9/__init__.py" --fail-under=100` _(la lista canónica vive en `nikodym.testing.regulatory`; el CI la consume vía `regulatory_coverage_include_arg()`)._
- Liviano: `uv run --no-sync python -c "import nikodym.core, sys; assert not [m for m in ('nikodym.data','pandera','pyarrow','pandas','nikodym.tracking','mlflow') if m in sys.modules]"`
- Build: `uv build` (wheel + sdist en `dist/`, gitignored).
- CI (GitHub): tras pushear, `gh run list -R nexolabs-gh/nikodym --limit 3` y `gh run view <id> -R nexolabs-gh/nikodym` → confirmar **verde** antes de cerrar el ítem.

## Dudas abiertas / bloqueos (preexistentes, no urgentes)
- **Pendiente normativo CMF:** haircuts/factores de descuento de garantías financieras del B-1 letra c) → F3.
- **Deudas con owner (Hito 0 §5):** pickle/joblib del `ArtifactStore` → F3; motor topológico DAG → F5; capa datos longitudinal → F4/F5; ~~matriz CI 3×3 + Hypothesis~~ **→ B4.3 hecho (matriz acotada) ; Windows/3.13 → B4.4/B4.5 (R0)**; ~~`assemble_run`/`ModelInventory` (CT-4) → B3~~ **✅ hecho en B3.1c**.
- **T2 (al materializar `StepAdapter.execute`):** propagar `_audit` al estimador envuelto; fijar derivación de `requires`/`provides` por dominio (SDD-06+). El gancho `StepAdapter` ya se ejercita desde `Study._resolve_step` en B2d.
- **Plugin mypy de pandera** (`plugins=["pandera.mypy"]`): NO se añadió; B2b/B2c/B2d cerraron sin necesitarlo. Reevaluar solo si aparece `DataFrameModel` (clases tipadas).
- Momento privado→público del repo (al terminar). Alias de email y Trusted Publishing OIDC en el release público (F1). _(El job `release` con OIDC ya está en el CI, inerte hasta el 1er tag `v*`.)_

## Repo
Privado en GitHub: **`nexolabs-gh/nikodym`** (https://github.com/nexolabs-gh/nikodym), branch `main`. Push directo a `main` autorizado en el cierre. Commits con co-autoría de Claude. **CI activo** (`.github/workflows/ci.yml`) — corre en cada push/PR; verde requerido para cerrar ítems.
