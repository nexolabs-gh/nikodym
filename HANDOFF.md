# HANDOFF

_Última actualización: 2026-06-25 · repo privado `nexolabs-gh/nikodym` · sobre commit `65a9204`_

## 🤖 Modo autónomo (rutina)
**Estado:** `IDLE`  _(IDLE = libre · `RUNNING [ts]` = corrida en curso. El lock de OS del wrapper garantiza exclusión; un `RUNNING` visto al arrancar = corrida muerta → rescatar, no abortar.)_
Última corrida: 2026-06-25 02:23 · ítem **B4.1** · `65a9204` · ✓ HECHO _(DanIA headless: worker Codex fresco creó el paquete `src/nikodym/testing/` (helpers públicos de testing, SDD-24): `estimator_checks.py` (`check_nikodym_estimator` con los **9 checks** obligatorios de familias propias + `all_nikodym_checks`), `strategies.py` (`nikodym_config_strategy` + `discriminated_union_tags` anti-drift; **Hypothesis import perezoso**, jamás top-level), `fixtures.py` (`minimal_study` + `dummy_step_config` Step dummy idempotente en REGISTRY + `golden_seed_sequence`), `reproducibility.py` (`assert_bitwise_reproducible`), `__init__.py`; + amplió el gate de núcleo liviano con `hypothesis` en `docs/AUTONOMY.md`. El maestro revisó el diff (R7) + revisión semántica vía fan-out read-only (**APTA-CON-MENORES**, 0 bloqueantes; import perezoso e núcleo liviano CONFIRMADOS; 9 checks mapeados al SDD-24 §7.2 con casos negativos) + leyó `fixtures.py`/tests para confirmar golden test fuerte (valores hardcodeados vs SeedManager), corrió los 7 gates — **510 tests, 100%** — commiteó y pusheó. **B4 troceado en B4.1✅/B4.2/B4.3.** Sigue **B4.2** (3 criterios de cierre del Hito 0).)_
Ciclo en la skill **auto-desarrollo** §2 (rescate §4); playbook en `docs/AUTONOMY.md`; bitácora en `AUTONOMY-LOG.md`. Worker Codex en tmux `nikodym`; maestro fresco por cron horario (:17).

## Backlog priorizado (cola autónoma)
> La rutina consume esto en orden, de arriba a abajo. Marca `[x]` al terminar; **NO** borres ni renumeres.
> Cada ítem es autocontenido = un módulo + sus tests, dejando los 4 gates verdes (cobertura 100%). El worker
> deja el árbol verde **sin commitear**; el maestro revisa, commitea y pushea (R7).
1. [x] **B2b.1** — `data/loading.py` (`DataLoader`): CSV/Parquet + passthrough de DataFrame con copia defensiva, `engine="pyarrow"` explícito, `backend="polars"` perezoso, `from_config`. SDD-02 §4. + `tests/unit/test_data_loading.py`. ✓ `6d699e2` (266 tests, 100%).
2. [x] **B2b.2** — `data/schema.py` (`SchemaValidator`): `SchemaConfig`→`pa.DataFrameSchema` (`import pandera.pandas as pa`), `validate(df, lazy=True)`→`DataValidationError` (reporte español). SDD-02 §7. ✓ `bdbea17` (282 tests, 100%). _Nota ratificada: `index_col`=nombre del índice existente (no `set_index`); `SchemaValidator` no exportado en `data.__init__`._
3. [x] **B2b.3** — `data/hashing.py` (`data_hash`): sha256 por bloques (`hash_pandas_object`), **endianness `<u8` explícito**, `-0.0→0.0`, `index=True`, defaults `hash_key`/`encoding`, golden cross-versión. SDD-02. ✓ `c7edaca` (293 tests, 100%). _Ratificado: ordena por índice (`sort_index` estable) → invariante a permutación de filas; eleva O(n) §7 a O(n log n), aceptado. Header de esquema versionado `nikodym.data_hash.v1`. No exportado en `data.__init__`._
4. [x] **B2b.4** — `data/special.py` (`SpecialValuePolicy`): centinelas→NaN + `special_mask` + `special_catalog`. SDD-02. ✓ `cf2487a` (302 tests, 100%). _Detección type-safe por dtype (evita FutureWarning con `filterwarnings=error`); `label` de `SpecialValueSpec` NO se propaga al `MaskedFrame` (catálogo = columna→centinelas, fiel a §4) → binning/SDD-06 releerá la config. No exportado en `data.__init__`._
5. [x] **B2c.1** — `data/target.py` (`TargetDefinition`): mini-DSL declarativo (allowlist cerrada de operadores, sin `eval`) → `target` `Int8` 1/0/NA + `label_status` categórico; precedencia exclusión>indeterminado>malo>bueno; ventana de desempeño no madurada→`excluido(ventana_incompleta)`. SDD-02 §4/§7. ✓ `3ea9523` (332 tests, 100%). _Consume `TargetConfig`/`Predicate`/`Rule`/`ExclusionRule`/`PerformanceWindow` ya existentes (B2a); valida compatibilidad dtype↔valor ANTES de comparar (lección B2b.4); `LabeledFrame`/`TargetDefinition` NO exportados en `data.__init__` (se cablea en B2d)._
6. [x] **B2c.2** — `data/partition.py` (`Partitioner`: temporal/random/cohort) con **Hypothesis** para determinismo y anti-leakage. SDD-02. ✓ `2e461c6` (359 tests, 100%). _Identidad de partición estable por `blake2b(root_seed:index)` (`byteorder="little"`, `-0.0→0.0`) → invariante a permutación e inserción, sin depender del orden posicional ni del `Generator` (`del rng`). OOT por corte (temporal/cohort) o fracción (random); HO por umbral de hash; rol `ttd` configurable. `Partition`/`PartitionResult`/`Partitioner` con `__all__` propio, NO reexportados en `data.__init__` (se cablea en B2d)._
7. [x] **B2d** — `data/step.py` (`DataStep @register("standard", domain="data")`) + `Study.run(steps=["data"])` end-to-end de datos. SDD-02 §4/§7 + CT-1. ✓ `1b01f6a` (370 tests, 100%). _Cableado vía `REGISTRY` con **import perezoso** de `nikodym.data` dentro de `Study._resolve_step` (núcleo liviano intacto, gate verde); reexports completos en `data/__init__.py`; nuevo `data/card.py` (`DataCardSection`) para el artefacto `data_card`; inyección de DataFrame en memoria por `("data","input_frame")` para correr sin I/O. **B2 COMPLETO.**_
8. **B3.1** — integración `audit`/`governance`/`tracking`/`api` (SDD-03/04, CT-4). Troceado un-módulo/commit:
   - [x] **B3.1a** — `audit/` (`JsonlAuditSink` JSONL append-only + `EnvironmentSnapshot`/`capture_environment` + `hashing` + `replay` + `AuditConfig` INFRA + `AuditError`) + cableado `NikodymConfig.audit` (hook diferido `_AUDIT_CONFIG_CLS`). SDD-03 §4.1/§5. ✓ `eb62d45` (387 tests, 100%).
   - [x] **B3.1b** — `governance/` (`ModelCard`/`DecisionRecord`/`ModelCardBuilder`, `ModelInventory` Protocol `@runtime_checkable`, `InventoryEntry`/`InventoryRecord`, `ScenarioLog`/`OverlayRecord`, `GovernanceConfig`) + cableado `NikodymConfig.governance`. SDD-03 §4.2. ✓ `8d4e543` (423 tests, 100%). _Cableado idéntico al patrón `audit`/`data` (hook diferido `_GOVERNANCE_CONFIG_CLS` + `field_validator` + campo `Any`-runtime/`GovernanceConfig`-`TYPE_CHECKING`). `governance` es sección **INFRA** (excluida de `config_hash`). `ModelCardBuilder` con `now`/`environment_provider` inyectables → golden bit-idéntico; `_add_months` stdlib (evita pandas, refuerza núcleo liviano). `ModelInventory` = solo Protocol + tipos (impl MLflow → B3.1c). No exportado en `core`._
   - [x] **B3.1c** — `tracking/` (`TrackingRecorder`/`TrackingSink`/`MLflowInventory`) + `api.assemble_run` (compone `FanOutSink`, resuelve inventario; CT-4) + `NikodymConfig.tracking`. SDD-04. ✓ `731f099` (456 tests, 100%). _MLflow import perezoso (núcleo liviano ampliado con `mlflow`/`nikodym.tracking`); `autolog=False`, aliases no stages, `fail_on_tracking_error` degrada a warning+no-op; idempotencia `register` por `(model_name,config_hash)`; `MLflowInventory` satisface el Protocol `ModelInventory` (B3.1b); `assemble_run` resuelve `NullInventory`/`MLflowInventory` por `governance.publish_to_inventory` (ya existía, B3.1b) con `MissingDependencyError` ruidoso; tests con fakes deterministas (mlflow NO instalado). **B3 COMPLETO.** 🟡 deuda: métricas duplicadas como tags `nikodym.metric.*` vs nota SDD-04 §4 (benigno, ver Decisiones)._
9. **B4** — `testing` + CI + 3 criterios de cierre del Hito 0. Último bloque de F0. Troceado un-trozo/commit (precedente B3.1):
   - [x] **B4.1** — `src/nikodym/testing/` (helpers públicos, SDD-24): `estimator_checks` (`check_nikodym_estimator` + 9 checks + `all_nikodym_checks`), `strategies` (`nikodym_config_strategy` + `discriminated_union_tags`; **Hypothesis import perezoso**), `fixtures` (`minimal_study`/`dummy_step_config`/`golden_seed_sequence`), `reproducibility` (`assert_bitwise_reproducible`) + gate liviano ampliado con `hypothesis`. ✓ `65a9204` (510 tests, 100%). _Hypothesis NO se expone como extra público (SDD-24): queda como dependency-group dev/test + import perezoso con mensaje. `dummy_step_config` registra Step dummy idempotente en REGISTRY global (no se limpia; benigno en F0). Deuda menor → F1 (ver Decisiones B4.1)._
   - [ ] **B4.2** — Los **3 criterios de cierre del Hito 0** (`_CONTRATOS-TRANSVERSALES.md` §4, líneas ~132-139): (1) test de **Step dummy con fan-in** (`requires` de 2 artefactos de 2 dominios → ejercita CT-1 y la validación pre-run del motor); (2) test de **payload estructurado dummy** (pasar `metric_sections` ECL-like por stage×escenario por `log_metrics`/`ModelCard` + `payload` no escalar por `OverlayRecord` → verifica la puerta de extensión CT-2); (3) **gate de cobertura regulatoria sin falsos verdes** (verificar que cada módulo declarado regulatorio EXISTE; hoy `0/0=100%` pasaría por vacuidad). Puede apoyarse en los helpers de B4.1 (`dummy_step_config`, etc.).
   - [ ] **B4.3** — CI: `.github/workflows/` con los jobs de SDD-25 §7 (`quality` ruff+mypy · `test` matriz **3 Python × 3 OS** = 3.11/3.12/3.13 × ubuntu/macos/windows · `coverage-regulatory` · `build` anti-copyleft · `lock-check` · `release` por tag/OIDC) + perfil Hypothesis `ci` (`derandomize=True`, `max_examples=200`, `deadline=None`). **⚠ Aislar:** es YAML, NO se valida con los gates locales (el job real corre en GitHub) → revisar sintaxis + lógica, no esperar verde local. **Cierra F0.**

## Estado actual
**Nikodym RiskLib — F0 (Fundación): B1 `core` ✅ + B2 `data` ✅ + B3 (audit/governance/tracking/api) ✅ + B4 ▶ EN CURSO (B4.1 `testing` ✅, B4.2/B4.3 pendientes).** F0 troceado en 4 bloques: B1 `core` ✅ · B2 `data` ✅ · B3 `audit`✅/`governance`✅/`tracking`✅/`api`✅ (CT-4) ✅ · **B4 ▶** troceado: **B4.1 `testing` ✅** (`65a9204`) · **B4.2 ▶ SIGUIENTE** (3 criterios cierre Hito 0) · **B4.3** (CI). B4 cierra F0.

**B3 por dentro (ítem 8 del backlog, troceado un-módulo/commit) — COMPLETO:** **B3.1a ✅** (`audit/`: `JsonlAuditSink` + `EnvironmentSnapshot` + `hashing` + `replay` + `AuditConfig` INFRA + cableado `NikodymConfig.audit`, `eb62d45`) · **B3.1b ✅** (`governance/`: `ModelCard`/`ModelCardBuilder` + `ModelInventory` Protocol + `InventoryEntry`/`InventoryRecord` + `ScenarioLog`/`OverlayRecord` + `GovernanceConfig` INFRA + cableado `NikodymConfig.governance`, `8d4e543`) · **B3.1c ✅** (`tracking/` MLflow: `TrackingRecorder`/`TrackingSink`/`MLflowInventory` + `api.assemble_run` `FanOutSink` CT-4 + cableado `NikodymConfig.tracking` INFRA, `731f099`). _(B2: B2a config · B2b loading/schema/hashing/special · B2c target/partition · B2d step+cableado, todo ✅.)_

Regla de oro vigente: **mixto-troncal-más-incremental** — cada módulo: programa → `ruff`+`mypy --strict`+`pytest`+cobertura verde → ajusta → sigue. Nunca avanzar en rojo. Reabrir un SDD por feedback del código es esperado y barato.

## Hecho en la última corrida (B4.1 · `src/nikodym/testing/` — helpers públicos de testing)
- **`src/nikodym/testing/` (PAQUETE NUEVO)** — superficie pública de helpers de testing de SDD-24. Primer trozo de B4 (de 3). Distribuible en el wheel; NO contamina el núcleo liviano.
  - **`estimator_checks.py`**: `check_nikodym_estimator(estimator) -> None` ejecuta los **9 checks** obligatorios de estimadores de familias propias (alternativa a sklearn `check_estimator`) + `all_nikodym_checks() -> list[tuple[str, Callable]]`. Los 9 mapean 1:1 al SDD-24 §7.2: `check_no_logic_in_init`, `check_get_params_mirrors_config`, `check_set_params_roundtrip`, `check_not_fitted_raises`, `check_fitted_attrs_suffix`, `check_from_config_roundtrip`, `check_validate_config`, `check_audit_default_null`, `check_reproducible`. `AssertionError` con mensaje en español por fallo.
  - **`strategies.py`**: `nikodym_config_strategy(*, sections=None, require_data=False) -> SearchStrategy[NikodymConfig]` + `discriminated_union_tags() -> dict[str, list[str]]` (cruce anti-drift de uniones discriminadas de sección vs el Registry). **Hypothesis import PEREZOSO** (dentro de funciones, jamás top-level; mensaje claro si falta, dev-only PEP 735 — confirmado por test con subprocess).
  - **`fixtures.py`**: `minimal_study()` (= `Study(NikodymConfig())`, DoD F0) + `dummy_step_config()` (registra un Step dummy **idempotente** en `REGISTRY`, con defensa anti-drift; devuelve `NikodymConfig` mínimo `run.steps=[]` — el schema F0 no admite sección `testing` dinámica) + `golden_seed_sequence(name, n)` (enteros dorados de `SeedManager(42)`; **golden test fuerte**: los tests fijan valores hardcodeados).
  - **`reproducibility.py`**: `assert_bitwise_reproducible(run, *, normalize=None)` (doble ejecución + comparación bit-exacta vía `pickle` proto 5, con normalización opcional).
  - **`__init__.py`** con `__all__` de la superficie pública (no importa hypothesis al cargarse).
- **`docs/AUTONOMY.md` (MOD)**: gate de núcleo liviano ampliado con `hypothesis` (core no debe arrastrarlo).
- **Tests** (`tests/unit/test_testing_estimator_checks.py`, `test_testing_strategies.py`, `test_testing_fixtures.py`, `test_testing_reproducibility.py`, NUEVOS): casos positivos + un negativo por CADA uno de los 9 checks; subprocess que prueba que importar `nikodym.testing` NO carga hypothesis; golden values hardcodeados de `SeedManager(42)`. Cobertura 100% honesta.
- **Verde total** (corrido y verificado por el maestro, R7): `ruff` + `ruff format` (99) + `mypy --strict` (**58 archivos**) + **510 tests** + **cobertura global 100%** (3002 stmts, 784 branches, 0 miss/partial, `--cov-fail-under=100`) + gate regulatorio 100% + núcleo liviano ampliado (no arrastra `hypothesis`) + `uv build`. + **revisión semántica vía fan-out read-only: APTA-CON-MENORES** (0 bloqueantes; import perezoso e núcleo liviano CONFIRMADOS; 9 checks fieles al SDD-24 §7.2 con casos negativos). El worker dejó el árbol verde **sin commitear**; el maestro revisó, commiteó (`65a9204`) y pusheó a `main`.
- **🟡 Observaciones menores (deuda NO bloqueante → F1):** (1) `test_golden_seed_sequence_equivale_a_seed_manager` es redundante/tautológico (compara la función con `SeedManager(42)` recalculado) — INOFENSIVO porque el golden real con valores hardcodeados (`test_..._devuelve_valores_exactos`) sí protege contra mutación de la semilla. (2) `dummy_step_config` deja el Step dummy en el `REGISTRY` global sin limpiar (idempotente + documentado; en suites largas de F1+ conviene un fixture con cleanup / `REGISTRY.unregister`). (3) `reproducibility._canonical_bytes` usa `pickle` (determinista para int/ndarray/float; documentar caveat de orden de dict/DataFrame para F1+). Ninguna bloquea; el SDD no las exige.

## En curso / a medias
Nada a medias. B4.1 cerrado, commiteado (`65a9204`) y pusheado; árbol limpio y verde. **B4 EN CURSO** (B4.1 `testing` ✅; faltan B4.2 criterios Hito 0 y B4.3 CI). Siguiente: **B4.2**.

## Próximos pasos
**Siguiente: B4.2** (ítem 9 del backlog) — los **3 criterios de cierre del Hito 0** (`_CONTRATOS-TRANSVERSALES.md` §4, líneas ~132-139): (1) test de **Step dummy con fan-in** (`requires` de 2 artefactos de 2 dominios distintos → CT-1 + validación pre-run del motor); (2) test de **payload estructurado dummy** (`metric_sections` ECL-like stage×escenario vía `log_metrics`/`ModelCard` + `payload` no escalar vía `OverlayRecord` → CT-2); (3) **gate de cobertura regulatoria sin falsos verdes** (verificar que cada módulo declarado regulatorio EXISTE; hoy `0/0=100%` pasaría por vacuidad — `provisioning/cmf|ifrs9` están vacíos). Apóyate en los helpers de B4.1 (`dummy_step_config`, `assert_bitwise_reproducible`, etc.). Método validado: SDD → worker Codex fresco un-trozo/commit → revisión + fan-out semántico + 7 gates + push (R7).
**Luego B4.3** (CI, cierra F0) — `.github/workflows/` con los jobs de SDD-25 §7 (`quality`/`test` matriz **3 Python × 3 OS**/`coverage-regulatory`/`build`/`lock-check`/`release`) + perfil Hypothesis `ci`. **⚠ Es YAML: NO se valida con gates locales** (el job real corre en GitHub) → revisar sintaxis+lógica, aislar en su propia corrida.
- **⚠ Nota corregida (era stale):** la matriz CI 3×3 + Hypothesis NO van diferidas a F1 — **SDD-25 §5/§7 las pone en B4** (B4.3). La línea vieja "matriz CI 3×3 + Hypothesis → F1" de "Dudas abiertas" quedó obsoleta.
- **Deuda B4.1 → F1 (3 menores, NO bloquean):** (1) quitar el test redundante/tautológico `test_golden_seed_sequence_equivale_a_seed_manager` (el golden hardcodeado ya protege); (2) `dummy_step_config` deja el Step dummy en el `REGISTRY` global sin limpiar → fixture con cleanup / `REGISTRY.unregister` para suites largas; (3) documentar el caveat de orden de `pickle` en `reproducibility._canonical_bytes`.
- **Deuda abierta de B3.1c (diferida, NO se tocó en B4.1):** decidir si `MLflowInventory.register` debe seguir duplicando las métricas como tags `nikodym.metric.*` o alinearse con SDD-04 §4 (métricas solo como *metrics del run*). + endurecer la doble escritura de tags (comentar intención) y unificar `_registry_db_backed` (DRY) en `tracking/inventory.py`+`recorder.py`. Benigno; resolver al consumir el inventario o en limpieza de F1.
- Pendiente de B3.1b (si se vuelve a tocar el model card): `deepcopy`/snapshot de `metric_sections` en `model_card.py`; congelar `ScenarioLog.path`.
- Cuando F0+ sume más dominios orquestables, **generalizar** el mapeo dominio→módulo de `Study._ensure_domain_registered`/`_coerce_domain_config` (hoy `if name == "data"`, ver deuda abajo).

Tras B4 (cierra F0): T2 scoring → F1 → release v0.1.0.

## Decisiones / contexto a recordar
- **B4.1 `testing` — decisiones SDD-24:** (a) **paquete `src/nikodym/testing/`** distribuible (va en el wheel) pero **NO contamina el núcleo liviano**: `import nikodym.core` no arrastra `hypothesis`; dependencia unidireccional `testing → core` (core nunca importa testing). (b) **Hypothesis NO se expone como extra público `[testing]`** (decisión del worker leyendo SDD-24): queda como `dependency-group` dev/test (PEP 735) + **import perezoso dentro de funciones** en `strategies.py` con mensaje claro si falta. Confirmado por test con subprocess. (c) gate de núcleo liviano (`docs/AUTONOMY.md`) ampliado con `hypothesis`. (d) los **9 checks** de `check_nikodym_estimator` mapean 1:1 a SDD-24 §7.2; cada uno con un test negativo dedicado. (e) **`golden_seed_sequence` con golden test fuerte**: los tests fijan valores hardcodeados de `SeedManager(42)` (no solo recalculan) → protegen contra mutación de la semilla/algoritmo. (f) `dummy_step_config` registra un Step dummy **idempotente** en el `REGISTRY` global (schema F0 no admite sección `testing` dinámica → `run.steps=[]`; el step se resuelve desde el registro). (g) `assert_bitwise_reproducible` compara vía `pickle` proto 5 con `normalize` opcional. (h) **🟡 deuda menor → F1** (ver Próximos pasos): test tautológico redundante, dummy no limpia el registry, caveat de pickle sin documentar. Ninguna bloquea.
- **B3.1c `tracking` — decisiones SDD-04 + CT-4:** (a) **paquete `tracking/` fuera de `core`** + `src/nikodym/api.py` como capa fina de orquestación (CT-4: `assemble_run` no vive en `core` para no contaminar el núcleo liviano). (b) **MLflow import perezoso** vía `utils/optional.require_extra("tracking","mlflow")` SIEMPRE dentro de métodos; `mlflow` en extra `[tracking]` de pyproject (≥2.10), NO en deps; gate de núcleo liviano ampliado con `mlflow`/`nikodym.tracking`. (c) **cableado idéntico a `audit`/`governance`**: hook `_TRACKING_CONFIG_CLS` + `field_validator` `_valida_tracking` + campo `Any`-runtime/`TrackingConfig`-`TYPE_CHECKING`; `tracking` es sección **INFRA** (ya en `INFRA_SECTIONS`). (d) **`MLflowInventory` implementa el Protocol `ModelInventory` de B3.1b** (no redefine); idempotencia por `(model_name, config_hash)` (D-TRK-7); `RegistryUnavailableError` importado de `governance.exceptions`. (e) **`assemble_run` resuelve inventario por `governance.publish_to_inventory`** (campo bool ya existente desde B3.1b, default False): True+extra→`MLflowInventory`; True+sin extra→`MissingDependencyError` ruidoso; si no→`NullInventory`. Compone `FanOutSink([JsonlAuditSink, TrackingSink])` reusando el de `core.audit`. (f) **`autolog=False` (regla dura D-TRK-3), aliases no stages (D-TRK-8), `fail_on_tracking_error=False` degrada a warning+no-op** — tracking jamás tumba el cálculo. (g) **reloj/cliente/módulo mlflow inyectables** (defaults reales) → golden bit-idéntico, y **tests con fakes deterministas** sin requerir mlflow instalado (cobertura 100% honesta). (h) **🟡 deuda NO bloqueante:** `register` duplica `entry.metrics` como tags `nikodym.metric.*` vs nota SDD-04 §4 (métricas solo como metrics del run) — benigno (solo-lectura); decidir en B4 si alinear o actualizar el SDD. + doble escritura de tags (cinturón-y-tirantes) y `_registry_db_backed` duplicado (DRY).
- **B3.1b `governance` — decisiones SDD-03 §4.2:** (a) **cableado idéntico a `audit`/`data`**: hook `_GOVERNANCE_CONFIG_CLS` + `field_validator` `_valida_governance` + campo `Any`-runtime/`GovernanceConfig`-`TYPE_CHECKING` (núcleo liviano intacto, mypy estricto). (b) **`governance` es sección INFRA** → excluida de `config_hash` (`INFRA_SECTIONS`): cambiar gobernanza no altera la identidad del experimento. (c) **`ModelCardBuilder` con `now`/`environment_provider` inyectables** (defaults reales) → model card golden bit-idéntico (misma lección que `EnvironmentSnapshot` de B3.1a). (d) **`_add_months` stdlib (`calendar.monthrange`)** en vez de `pandas.DateOffset` del SDD §7.1.f → `next_review_date` sin arrastrar pandas en runtime (mejora sobre el SDD, refuerza núcleo liviano); el `data_card` insumo se referencia como `Any`/`TYPE_CHECKING`. (e) **`DecisionRecord` con campos en español** (`regla`/`umbral`/`valor`/`accion`, 1:1 con `core.log_decision`; tolera alias histórico `acción`). (f) **`ModelInventory` = solo Protocol `@runtime_checkable` + `InventoryEntry`/`InventoryRecord`** (contrato CT-4); la implementación `MLflowInventory` es B3.1c, que DEBE satisfacer este Protocol. (g) **`RegistryUnavailableError` definido en `governance/exceptions.py`** (junto a `GovernanceError`) — el SDD-04 confirma que vive en governance, no se duplica en tracking. (h) **2 obs menores de revisión, deuda defensiva NO bloqueante**: copia superficial de `metric_sections` (vs snapshot profundo de `metrics`); `ScenarioLog.path` mutable. El SDD no las exige; candidatas a endurecer en B3.1c.
- **B3.1a `audit` — decisiones SDD-03 §4.1/§5:** (a) **JSONL canónico append-only**: `sort_keys=True`+`separators=(",",":")`+`ensure_ascii=False` → línea byte-estable y legible (tildes/ñ); el sink reabre siempre en modo `"a"`, nunca reescribe (test lo prueba). (b) **`capture_environment` con providers inyectables** (`now`/`version_provider`/`python_version_provider`/`platform_provider`) — desviación INTENCIONAL sobre el pseudocódigo §4.1, que §9/§11 exige para snapshot determinista (golden bit-idéntico). (c) **`hash_dataframe` REUSA `data_hash`** (import perezoso de `nikodym.data`), no reimplementa; `algo` distinto de `sha256` falla ruidoso (contrato regulatorio fija sha256). (d) **replay tolerante a truncamiento**: cola corrupta (escritura a medias) → `warning` + eventos válidos previos; corrupción seguida de línea válida → `AuditError` (trail manipulado, no cola de crash). (e) **cableado `NikodymConfig.audit`**: hook diferido `_AUDIT_CONFIG_CLS` + `field_validator` + campo `Any`-en-runtime / `AuditConfig`-bajo-`TYPE_CHECKING` (núcleo liviano intacto, mypy estricto), idéntico al patrón de `data`. (f) **`audit` es sección INFRA** → excluida de `config_hash` (`INFRA_SECTIONS`): cambiar auditoría no altera la identidad del experimento. (g) `AuditConfig.capture_environment`/`tracked_packages` quedan inertes hasta que `api.assemble_run` (B3.1c) los consuma — esperado, no es deuda.
- **B2d `DataStep` + cableado `Study.run` — decisiones SDD-02 §4/§7 + CT-1:** (a) `@register("standard", domain="data")` — discriminador `type="standard"` (default v1), coherente con el SDD §4. (b) **núcleo liviano preservado**: `core/study.py` NO importa `nikodym.data` a nivel de módulo; el `DataStep` se autorregistra al importar `nikodym.data`, y `Study._resolve_step` lo fuerza con un `importlib.import_module("nikodym.data")` **perezoso dentro del método** (`_ensure_domain_registered`) → el gate `import nikodym.core` sin arrastrar data/pandera/pyarrow/pandas sigue verde. (c) `_default_step_names` deriva el pipeline v1 de secciones activas (`["data"]` si `config.data`), en **orden de declaración** (CT-1; scheduler topológico diferido a F5). (d) el resolver acepta `Step` nativo o adapta un `BaseNikodymEstimator` vía `StepAdapter` (gancho T2). (e) **inyección de datos en memoria** por artefacto `("data","input_frame")` (`INPUT_FRAME_KEY`) cuando `cfg.load.source is None` → end-to-end sin I/O; `ConfigError` si falta fuente o el inyectado no es `pd.DataFrame`. (f) `data_card` se emitió como módulo propio `data/card.py` (`DataCardSection`) — construido en `DataStep`, leído por governance (SDD-03). (g) `data/__init__.py` cierra la **reexportación diferida** de B2b/B2c (SchemaValidator, data_hash, SpecialValuePolicy/MaskedFrame, TargetDefinition/LabeledFrame, Partitioner/PartitionResult) + DataStep/DataCardSection.
  - **🟡 Deuda menor (para B3+):** `Study._ensure_domain_registered` y `_coerce_domain_config` tienen el mapeo dominio→módulo hardcodeado como `if name == "data"`. Pragmático con un solo dominio orquestable; al sumar binning/scoring/etc. en B3+ conviene generalizarlo (tabla dominio→paquete o entry-points). NO bloquea (honesto, cubierto, el SDD difiere el scheduler a F5).
- **B2c.2 `Partitioner` — decisiones SDD-02 §4/§7:** (a) **alcance reducido**: `PartitionConfig`/`TemporalSplitConfig`/`RandomSplitConfig`/`CohortSplitConfig` YA existían en `data/config.py` (B2a) → la corrida solo creó `partition.py` + tests, sin tocar config (mismo patrón que B2c.1 con `TargetConfig`). (b) **identidad estable por observación, NO por posición ni RNG**: la asignación deriva de `blake2b(f"{root_seed}:{idx}")` (`digest_size=8`, `person=b"nikodym"`, `byteorder="little"`, `-0.0→0.0`, MultiIndex/Timestamp serializados) → un `uniform01` por fila; el `np.random.Generator` se recibe (firma estable para sorteos futuros) pero **NO** participa en la identidad (`del rng`) → invariante a permutación de filas e inserción de filas nuevas — la propiedad anti-leakage clave, verificada con Hypothesis. (c) **OOT siempre por corte** (temporal: fecha ≥ `oot_from`; cohort: cohorte reservada) **o por fracción** (random); **HO** por umbral de hash dentro del in-time; **Dev** el resto. (d) **anti-leakage temporal**: `_validate_temporal_precedence` exige `max(fecha Dev/HO) < min(fecha OOT)` o `DataValidationError`. (e) filas no modelables (`indeterminado`/`excluido`/target NA) → `fuera_de_modelo`; rol `ttd` superpuesto, configurable (`ttd_includes_excluded`). (f) `suggest()` propone estrategia editable (fecha→cohorte→random) preservando opciones globales. (g) `Partition`/`PartitionResult`/`Partitioner` con `__all__` propio, reexportados en `data/__init__.py` en B2d.
- **B2c.1 `TargetDefinition` — decisiones SDD-02 §4/§6/§7:** (a) **alcance reducido**: `TargetConfig`/`PerformanceWindow`/`Predicate`/`Rule`/`ExclusionRule` YA existían en `data/config.py` (B2a) y `DataConfig.target` también → la corrida solo creó `target.py` + tests, sin tocar config. (b) **mini-DSL sin `eval`**: evaluador propio con allowlist cerrada de 10 operadores; `Rule = (AND all_of) AND (OR any_of)`; un `else` defensivo cubre el caso imposible post-validación (`# pragma: no cover`). (c) **precedencia** exclusión>indeterminado>malo>bueno aplicada en orden; la primera exclusión que matchea gana el motivo en `exclusions_by_reason`. (d) **filas no clasificadas** (con `good_rule` explícita y sin match de ninguna clase) → `indeterminado` (target NA), conservador. (e) `target` `Int8` nullable + `label_status` `pd.Categorical` con categorías fijas. (f) `_validate_output_columns` impide sobrescribir columnas del cliente (`DataValidationError`). (g) `LabeledFrame`/`TargetDefinition`/`TargetSummary` con `__all__` propio, reexportados en `data/__init__.py` en B2d.
- **B2b.4 `SpecialValuePolicy` — decisiones SDD-02 §4/§7:** (a) **detección type-safe por dtype** (`_is_comparable`): NO se compara un centinela contra columna de dtype incompatible (sentinel numérico vs `datetime`/`string`, o cualquier sentinel vs `bool`) — necesario porque `filterwarnings=error` convierte el `FutureWarning` de comparación cross-dtype en fallo. (b) El **`label`** de `SpecialValueSpec` **NO se propaga** al `MaskedFrame`: `special_catalog` es `columna→[centinelas]` (fiel a la firma §4); el mapeo centinela→`label` lo hará binning (SDD-06) releyendo la config — anotado para que SDD-06 no lo asuma resuelto aquí. (c) Centinelas detectados sobre el `df` **original**, no sobre el frame ya parcialmente reemplazado. (d) `MaskedFrame` Pydantic con `arbitrary_types_allowed=True`. (e) reexportado en `data/__init__.py` en B2d.
- **B2b.3 `data_hash` — ratificaciones SDD-02 §4/§7/§9:** (a) `data_hash` **ordena por índice** internamente (`sort_index(mergesort)`) → hash invariante a permutación de filas con el mismo índice (el índice ES el id de observación, §9); esto eleva la complejidad O(n) de §7 a **O(n log n)** por el sort — aceptado. (b) **Endianness `<u8` explícito** sustituye el `.values.tobytes()` (endian-nativo) del pseudocódigo §4. (c) Normalización `-0.0→0.0` + pin de `hash_key`/`encoding`/`categorize`. (d) Header de esquema **versionado** `nikodym.data_hash.v1`. (e) reexportado en `data/__init__.py` en B2d.
- **Desviaciones de SDD-02 a ratificar (B2a; integrador):**
  - **`model_rebuild()` del SDD §5 NO funciona**: Pydantic 2.13 no re-narra un campo ya resuelto. Reemplazo: **hook `_DATA_CONFIG_CLS` (módulo `schema`) + `field_validator`**; `nikodym.data` lo puebla al importarse.
  - **`Predicate.value` SIN `strict=True`**: `Field(strict=True)` revienta sobre una unión en Pydantic 2.13 y rompería list→tuple del round-trip. El **modo unión smart** (default) + orden `bool|int|float` ya evita la coerción.
  - **Defaults de `Field` en keyword `default=`** (no posicional): mypy sin plugin pydantic no reconoce un default posicional.
  - **`pandera>=0.24` con `import pandera.pandas as pa`** (NO `import pandera`): el top-level emite `FutureWarning` desde 0.24. **`pyarrow>=14`**.
  - Acumuladas previas (B1): `pandas-stubs`/`joblib.*` overrides; `StepAdapter` `*, requires/provides`; resolución de pasos diferida a T2 (ahora materializada en B2d); `mypy` en 3.12 no 3.11.
- **Hook `_DATA_CONFIG_CLS` (cómo razonar sobre tests):** es global de proceso. Al colectar, cualquier test que importe `nikodym.data` lo deja seteado para toda la sesión. Los tests core-only (`test_config_schema`, `test_config_loader`) lo neutralizan con `monkeypatch.setattr(schema, "_DATA_CONFIG_CLS", None)`; `test_data_config` lo fija a `DataConfig`. Con hook None → `data` es blob JSON-canónico opaco; con hook seteado → se coacciona a `DataConfig` (extra=forbid). B2d depende de este coacheo en `Study._coerce_domain_config`.

## Callejones sin salida / no reintentar
- **NO `from nikodym.data...` a nivel de módulo en `core/`** (B2d) — rompe el núcleo liviano (gate `import nikodym.core` sin data/pandera/pyarrow/pandas). El registro de dominios se puebla por **import perezoso DENTRO de método** (`importlib.import_module` en `Study._ensure_domain_registered`), nunca en el top-level de core. **Igual para `audit`** (B3.1a): `NikodymConfig.audit` es `Any` en runtime + hook `_AUDIT_CONFIG_CLS` poblado al importar `nikodym.audit`; NUNCA `from nikodym.audit import ...` en `core/` a nivel módulo (`audit` arrastra pandas vía `hash_dataframe`). Lo mismo aplicará a `governance`/`tracking` en B3.1b/c (y `mlflow` deberá sumarse al gate liviano en B3.1c).
- **NO comparar valor vs columna de dtype incompatible sin guardia** (B2b.4 + B2c.1) — `series.eq(x)` / `.lt(x)` cross-dtype (numérico vs `datetime`/`string`/`bool`, value escalar vs `category` ordenada, etc.) emite `FutureWarning` → rompe con `filterwarnings=error`. Filtrar por dtype ANTES de comparar (`_is_comparable` en special, `_validate_value_compatible` en target) y `comparison.fillna(False).astype(bool)` para absorber el NA del resultado.
- **NO usar `model_rebuild()` para narrar `NikodymConfig.data`** — no funciona en Pydantic 2.13. El hook+validador es la vía.
- **NO `Field(strict=True)` sobre uniones** en Pydantic 2.13 — usar el modo smart (default).
- **NO defaults posicionales en `Field(...)`** si el modelo es target de `default_factory` — keyword `default=`.
- **`ruff` respeta `.gitignore`**: si un paquete fuente cae bajo un patrón ignorado, ruff lo SALTA silenciosamente (verde falso). Verificar el wheel/`git status` cuando se añade un subpaquete nuevo. El patrón de datos es `/data/` (raíz), no `data/`.
- **NO `import pandera as pa`** (top-level) — `FutureWarning` rompe con `filterwarnings=error`. Usar `import pandera.pandas as pa`.
- **NO `.values.tobytes()` para hashes reproducibles** (B2b.3) — es endian-NATIVO. Forzar little-endian: `hash_pandas_object(...).to_numpy(dtype="<u8", copy=True).tobytes()`. Normalizar `-0.0→0.0` y pinear `hash_key`/`encoding`/`categorize`.
- Heredados de B1 (siguen vigentes): NO cobertura por submódulo (`--cov=nikodym.core.audit` → doble-load numpy); `ArtifactStore.keys()` → `# noqa: SIM118`; colisión módulo/función `migration` (usar `from ... import X`); workflow JS: `parallel()` ya devuelve array, prompts sin backticks internos, `Date.now()`/`Math.random()` prohibidos.

## Comandos verde
- Sync: `uv sync --no-default-groups --group test --group lint --python 3.12`
- Verificar: `uv run --no-sync ruff check . && uv run --no-sync ruff format --check . && uv run --no-sync mypy && uv run --no-sync pytest -q --cov=nikodym --cov-report=term-missing`
- Gate regulatorio (100%): `uv run --no-sync coverage run -m pytest && uv run --no-sync coverage report --include="*/nikodym/core/exceptions.py,*/nikodym/core/seeding.py,*/nikodym/provisioning/cmf/__init__.py,*/nikodym/provisioning/ifrs9/__init__.py" --fail-under=100`
- Liviano: `uv run --no-sync python -c "import nikodym.core, sys; assert not [m for m in ('nikodym.data','pandera','pyarrow','pandas') if m in sys.modules]"`
- Build: `uv build` (wheel + sdist en `dist/`, gitignored).

## Dudas abiertas / bloqueos (preexistentes, no urgentes)
- **Pendiente normativo CMF:** haircuts/factores de descuento de garantías financieras del B-1 letra c) → F3.
- **Deudas con owner (Hito 0 §5):** pickle/joblib del `ArtifactStore` → F3; motor topológico DAG → F5; capa datos longitudinal → F4/F5; ~~matriz CI 3×3 + Hypothesis → F1~~ **CORREGIDO: → B4.3** (SDD-25 §5/§7 las pone en B4, no F1); ~~`assemble_run`/`ModelInventory` (CT-4) → B3~~ **✅ hecho en B3.1c**.
- **T2 (al materializar `StepAdapter.execute`):** propagar `_audit` al estimador envuelto; fijar derivación de `requires`/`provides` por dominio (SDD-06+). El gancho `StepAdapter` ya se ejercita desde `Study._resolve_step` en B2d (rama `BaseNikodymEstimator`).
- **Plugin mypy de pandera** (`plugins=["pandera.mypy"]`): NO se añadió; B2b/B2c/B2d cerraron sin necesitarlo (`schema.py` usa `DataFrameSchema` imperativo). Reevaluar solo si aparece `DataFrameModel` (clases tipadas).
- Momento privado→público del repo (al terminar). Alias de email y Trusted Publishing OIDC en el release público (F1).

## Repo
Privado en GitHub: **`nexolabs-gh/nikodym`** (https://github.com/nexolabs-gh/nikodym), branch `main`. Push directo a `main` autorizado en el cierre. Commits con co-autoría de Claude.
